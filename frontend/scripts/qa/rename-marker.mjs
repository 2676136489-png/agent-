/**
 * 改名落地时点标记（QA 维护）。
 *
 * 为什么需要它：scripts/qa/out/ 下的产物是「某个时间点的真实抓帧」。
 * 文案改名之后，旧抓帧仍然存在、仍然可解析、字段也仍然齐全 —— summarize.mjs
 * 会把它们当有效数据，输出一个看起来很像结论的判定（实测：`失败计数: 2`、
 * `❌溢出+176px`）。而那个结论描述的是改名前布局，会被误读成「改名引入了溢出」，
 * 于是有人去修一个不存在的布局问题，真正要验的「改名后是否仍溢出」反而没人跑。
 *
 * 所以产物必须带时间下限：早于该时点的抓帧一律判为 STALE-DATA，且拒绝出结论。
 * 注意是「拒绝出结论」而不是「加个星号仍然出结论」—— 注记挡不住工具，工具得自己拒绝。
 */

/**
 * 改名（导航项「研究工作流」→「深度研究」）在 src/ 的落地时点。
 * 取值：2026-09-25 16:33 (CST, UTC+8)。
 * 依据：src/App.tsx 与 src/features/workflow/ResearchWorkflow.tsx 在该分钟被改写为「深度研究」；
 *       out/ 下全部产物均早于此刻（最新 13:35）。
 *
 * 变更时点时必须同步改这里，否则判定会失真。
 * 例：若日后再次改名，把本常量更新为新的落地时刻。
 */
export const RENAME_LANDED_AT = Date.parse('2026-09-25T16:33:00+08:00')

/** 文件名统一带上改名前才有的旧名，可据此直接判定为历史快照。 */
export const PRE_RENAME_LABEL = '研究工作流'
