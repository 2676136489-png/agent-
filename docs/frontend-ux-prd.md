# AI Research Workspace · 前端 UX 与信息架构 PRD

> **修订说明**：本文件在 v1.5 阶段曾因一次脚本错误被整体清空并**重建**。重建依据本轮会话中已核实过的全部结论、证据与裁决，但**行文可能与原稿有细微出入**。若你发现任何条目与你所知的结论不符，**以本会话的裁决记录为准，并请告知我更正**。

---

## 0. 原始需求分解

| 原始诉求 | 本文档落点 |
|---|---|
| 交互与体验深化（骨架屏/微交互/键盘快捷键/操作反馈，减少等待焦虑） | §5.1（A1–A8）、§5.2（B1–B6）、§5.3（C1–C8） |
| 视觉细节打磨（排版节奏、深色主题、过渡动效、图标与留白） | §6.1–§6.5 |
| 信息架构重组（页面层级、导航分组、入口逻辑，缩短上手路径） | §4 |
| 用户三条诉求的覆盖校验 | §1 产品目标 G1/G2/G3 |

---

## 1. 产品目标

| 目标 | 描述 | 对应诉求 |
|---|---|---|
| **G1 缩短上手路径** | 新用户从打开到「看到已填好示例问题的研究界面」**≤4 次点击、≤30 秒** | 信息架构重组 |
| **G2 消除等待焦虑** | 启动研究后 **≤1 秒**出现研究看板，此后**任意 3 秒内界面有可见变化**；操作**每步都有反馈** | 交互深化 |
| **G3 观感达到成品级** | 四档视口 × 9 页零横向溢出；明暗双主题对比度扫描可疑项 **= 0** | 视觉打磨 |

---

## 2. 硬约束（本轮不许越界）

### 2.1 不改的东西

| 项目 | 约束 |
|---|---|
| 功能、路由、业务逻辑 | 不加 react-router、不加组件库、不引 CSS 框架；`useState<PageKey>` 单页切换结构不变 |
| 样式结构 | 必须继续用 `src/styles/*.css` 的分层结构与现有设计令牌，**不新造硬编码值** |
| 不改 API 调用与数据结构 | 不依赖后端「搜索用量」字段。**该字段本轮已落地**（`QuotaSnapshot`），但本轮**不做该 UI**（见 §12 与 Q8） |
| 不引入新依赖 | Toast、图标、ETA 均为手写；状态管理沿用模块级单例模式 |

### 2.2 可以新增的东西

新增 UI state、新增 CSS 类、新增纯展示组件、新增手写 SVG 图标、新增 `localStorage/sessionStorage` 键。

### 2.3 本轮三条硬约束（team-lead 下达，前端架构师必须遵守）

1. **进度条永不为"假满格"**：`fill = min(len(steps) / 上界, 0.95)`。run 真正结束前最高 95%，「100%」这一格 reserve 给终态。**欠显示优于透支**——假装满格会让用户判定"卡住了"。
2. **陈旧过滤只存在于唯一谓词**：`awaiting_approval` 无条件豁免（详见 §5.2 B3-1）。
3. **不写待字段的骨架**：后端未交付的字段，本轮不铺占位 UI。

---

## 3. 当前体验诊断（逐条带证据）

| ID | 问题 | 证据 |
|---|---|---|
| **D1** | 导航 9 项平铺，无主次 | `App.tsx` 导航数组；9 个功能页宽度等同 |
| **D3** | 按钮反馈缺失 | `.button--primary:disabled`（`components.css:179-184`）仍是主色底 + `opacity:.5`，与主按钮几乎一样 |
| **D4** | 全局无 toast | 全站仅 `ResearchWorkflow.tsx:347` 一处刷新失败提示 |
| **D5** | **启动后界面长时间静止**（最大缺口） | `ProgressTrack` 位于 `RunView` 内（`ResearchWorkflow.tsx:602`），而 `run` 来自 `startResearch` 返回值；启动期 `run === null` → **7 节点进度轨完全不渲染**，内容区只剩一行「等待事件…」（`:487`） |
| **D6** | 错误无下一步指引 | `ErrorState.tsx:20-37` 支持 `tips` 入参，但**所有调用点都没传**（`ResearchWorkflow.tsx:463`、`KnowledgeBase.tsx:117/191`、`RunHistory.tsx:82`、`Reports.tsx:98`、`Settings.tsx:161`）→ 只剩一个「重试」按钮 |
| **D7** | 键盘可达性缺失 | 全库 `:focus-visible` **0 处**；`App.tsx:100-115` 全局键盘处理只有一条：Esc 关抽屉 |
| **D8** | 内联样式 37 处 | 违反"不新造硬编码值"约束 |
| **D9** | 深色主题 6 处对比度不达标 | 白字 `#fff` 于 `--accent` `#2997ff` ≈ **2.4:1**（低于 AA 4.5:1）；骨架 `--surface-alt` `#161617` 于 `--page` `#000` ≈ **1.2:1** |
| **D10** | 动效未覆盖 reduced-motion | `responsive.css:111-128` 已覆盖 `[data-reveal]`、`main`、`.skeleton`，但**未覆盖**：`.collapsible-card__body`（`components.css:1033`）、`.modal`（`pages.css:548`）、`.nav-scrim`（`layout.css:202`）、`.nav-drawer`（`layout.css:219`）、`.badge--running::before` 的 `pulse`（`components.css:727`） |
| **D11** | 首屏路径长 | 导航 9 项平铺，新用户需自行判断优先级 |
| **D12** | 教程与导航不一致 | `Tutorial.tsx:115` 明确写「平台由**五个模块**组成」，但导航有 9 项——**教程在教一套与导航不符的地图**，而这正是新用户的第一份阅读材料 |
| **D13** | 图标不统一 | 存在多套图标实现 |
| **D14** | 间距无刻度 | 存在 3px/5px 等非令牌值 |
| **D15** | **跨页运行状态不可见** | `[OBS-1]` 自动恢复（`ResearchWorkflow.tsx:25-49`，`sessionStorage['arw-last-thread']`）与模块级 SSE 连接都在正常工作，但**导航里没有任何"研究中"指示**，从别的页面完全无感；回到原页也是静默载入，无"已恢复上次运行"的解释 |

---

## 4. 信息架构重组（核心）

### 4.1 分组方案

| 组 | 页面 | 说明 |
|---|---|---|
| 家 | **概览** | `dashboard`。它不是"一个功能"而是**仪表盘**：承担后端连接状态（`Dashboard.tsx:109-116`）与全局运行状态。所有成熟 Web App 的 Home 都在导航首位 |
| ① 研究 | `workflow` ★主 / `research` / `agent` / `knowledge` | |
| ② 结果 | `reports` / `evaluation` | |
| ③ 系统 | `settings` / `tutorial` | |

> **未采纳的分组**：team-lead 提示的「工作时间 / 探索 / 运维」。理由：中文语境下"工作时间"隐喻不直观，且"探索"与实际动作（跑研究、传资料、问答）不符。**「研究 / 结果 / 系统」直接对应页面动作，零解释成本。**

### 4.2 关键改名（Q1，已批准）

`workflow`「研究工作流」→ **「深度研究 ★推荐」**。

理由：它与同组的 `research`「研究规划」并列时，两个"研究"会造成严重混淆；导航的主次区分**必须靠名字表达**。"Deep Research" 已在用户词汇里，且与首页 hero、主 CTA「开始研究」一致。**只改 label 与 3 处 eyebrow，不改路由 key。**

### 4.3 三档视口的呈现方式（回答"抽屉里放几项"）

**9 项全部进抽屉，但按 3 个组标题分层，`深度研究` 置顶并带「推荐」徽标。**

抽屉是纵向富余的容器——「全量 + 分组」优于「折叠掉一半」。与现状的差别不在项数，而在：① 三个组标题 ② 视觉主次 ③ 主入口在首屏可见。

### 4.4 「下一步」引导的五种机制

1. 主次视觉层级（★推荐 + 推荐徽标）
2. 会话内页面记忆（`sessionStorage`，`App.tsx:74`）
3. 上手进度条（`localStorage['arw-onboarded']`，`Dashboard.tsx:74-88`）
4. 全局运行状态指示器（B3）
5. 命令面板（⌘K，C3）

### 4.5 首屏

概览 hero 承载后端连接状态；新访客主 CTA 变「3 步开始研究 →」。

---

## 5. 交互体验深化清单

### 5.1 等待感（对应 R1）

| ID | 需求 | 优先级 | 现状与改动 |
|---|---|---|---|
| **A1** | **启动即出研究看板** | **P0** | 解决 D5：`run === null` 期间也要渲染 `.run-board` 骨架（7 节点占位），绝不能只剩一行「等待事件…」 |
| **A2** | **分阶段可见** | **P0** | 用 SSE 已完阶段驱动当前阶段文案；**节点进度轨始终可见**（`ResearchWorkflow.tsx:186` 的 `buildPhaseTimeline` 从 SSE 事件推导，非字面量清单）<br>**›› 节点/阶段清单的唯一来源（v1.10，勿再写成字面量）**：`backend/app/graph/graph.py:80-87` 的 `add_node` 调用顺序是**唯一真相来源**（当前 7 个主节点 + `fail`）。**`STAGE_ORDER` 不存在**（已核实：`backend/tests/` 零命中，**没有任何测试守着这个对应关系**）。前端只**引用**，不存副本——**禁止手抄第二份阶段清单**，改图时一律回查 `add_node`。<br>**›› 对后端的约束（v1.10）**：阶段顺序若需复用（如阶梯分母、进度语义），应**从图上推导**而非手写常量，并补一条断言锁住（backend-pm 已按 team-lead 核定执行） |
| **A3** | **预计剩余时间 ETA** | P1 | **数据源（v1.5 定案，零后端依赖）**——历史样本用 `GET /api/graph/runs?status=completed&limit=5` 的 `updated_at - created_at`，`created_at` 后端本就在返回（`run_store.py:114` → `routes/graph.py:87` 裸 `list[dict]` 直达）。展示「预计还需 2 分 10 秒」，**必须附一行口径小字「（含报告确认时间）」**（见 A3-1 ④）；**ETA 必须单调不回落**。首次运行无历史 →「首次运行，一般 2–4 分钟」 |
| **A3-1** | **ETA 样本口径 + 精度前提** | P1 | ① **只取 `status==='completed'`**；② **按问题长度分档**（<40 / 40–80 / >80 字），同档取最近 3 条；③ **上限 5 条，取中位数**。<br>**④（v1.5 重定性：口径问题，非缺陷）**：`graph.compile(..., interrupt_before=["write"])`（`graph.py:115-118`）**无条件生效——每一轮研究都要等人确认**。所以 `updated_at - created_at` 算的是**总墙钟时间**，这正是用户问「还要多久」时真正想知道的（「我什么时候拿到最终报告」）。**算法没有偏差，只是口径从未对用户说明**⇒ 修法是文案（ETA 附小字），不是改算法。<br>· **⚠️ 边界情况（P2 观察）**：若当前 run **已停在确认页**，「还需 3 分钟（含确认时间）」是别扭的——确认时间不是"还剩"而是"正在进行"，此时更该说「继续后约 30 秒出报告」。本轮先按统一文案执行。<br>**›› 因果链（必须写进文档）**：ETA 敢用真实数据的前提是**样本里没有僵尸 run**。僵尸 run 的 `updated_at` 是重启前的旧时间，混进来会把中位数整体拉高——**界面不会有任何异常提示，只是 ETA 悄悄越算越长**。所以 `listRuns` 的僵尸过滤是 A3 精度的**前置依赖，不是可选项**。<br>**v1.3 定案**：`created_at` 后端本就在返回 ⇒ 前端只在 `types/graph.ts` 加一行 `created_at: string`；**删除** `localStorage['arw-run-durations']` fallback 与「首屏空白 5 秒后出现 ETA」降级分支。<br>**取值兜底（必做）**：`created_at` 缺失或解析不出有限数值 → **不渲染 ETA**（退回「研究中」），**绝不渲染 `NaN 分钟` / 负数 / `Infinity`**。 |
| **A3-2** | **ETA 精度提升（不依赖本轮）** | P2 | backend PRD P1-9 计划把 `nodes.py:109` 丢掉的 `latency_ms` 加回并在 `_to_response` 累加。若落地，ETA 入参换成 `run.duration_ms` |
| **A4** | **历史事件预览前置** | P1 | 现状历史只在页面最底 `.page__status`（`:574-576`）。改：`RunHistory` 移入 `.run-board`，默认显示最近 3 条并高亮当前 run |
| **A5** | **进度百分比 / 步数** | P2 | **›› 标题旁标注（frontend-architect 措辞，照录）：此数为建模估计，非后端权威总数，亦不构成上界保证；真正的保证是 0.95 钳制 + AC2c。**<br>三个「非/不」缺一不可：`非权威总数`挡住"拿它当真相"，`不构成上界保证`挡住"拿它当安全上界"，最后一句把责任人交给真正的保险。**函数仍叫 `computeRunUpperBound`，靠注释与这行标注挡误用，不改名**（改名会让封闭名单里的引用全乱）。<br>· 文案**只显分子不显分母**：「已完成 3 个步骤」<br>· **分母推导，不写死常量**（`run.steps` 即 `types/graph.ts:50` 累计型字段，`len()` 即已完步骤数）<br>`上界 = maxIterations + 4 × MAX_VERIFY_ATTEMPTS + 2`<br>（frontend-architect 更正，**我 v1.2/v1.5 的公式是错的**，见下方"两处错"）<br>**›› 不许跳变（v1.11 新增，frontend-architect 用 team-lead 那条长效判据复审 §12.5 时挖出来的）**：AC2b 只锁「不满 100%」，**锁不住「从 78% 跳到 95%」这条路径**。上界与滑块成**反比**，所以任何一次拖动都会让 `fill` 跳，方向还不一样——向上拖（3→8）**进度倒退**（38.5% → 27.8%），向下拖（8→3）**虚假跃进**（77.8% → 95%，离终点还差 4 步却显示接近满）。**两种都是"条动了但 run 什么都没做"**，与 AC2c 读成"卡住了"是同一类心理机制：`fill` 的可信度被一次无关操作击穿。<br>**根因不是"值算错了"，是 A5 自己留的缝只做了一半**：`resolveProgress(run)` 的**分子来自 run，分母却来自 UI 当前值**——而 run 的真实配置在启动时就已快照（`ResearchWorkflow.tsx:341` 的 `startResearch({ question, maxIterations, threadId })`）。**用 UI 当前值当分母，等于拿一个描述不了这个 run 的数去算它。**<br>**两道修，职责不同，都要做**：<br>① **`disabled={hasStream}`（P0，一行，消成因）**：滑块在 run 期间本就**无任何作用**——`:341` 已把 `maxIterations` 快照进本次 run，运行期调它对图毫无影响。**一个不改结果的控件还能被拖动，是在骗用户。**<br>**⚠️ 门控必须是 `hasStream`，不能是 `running`（v1.12 更正，写错过一次）**：`running`（`:326/341/353/359/366`）**只覆盖 `startResearch()` 那一次 API 请求的往返**；真正的运行是靠 SSE 持续 1~2 分钟，那时 `running` 已是 `false`。**只写 `disabled={running}` 只能挡住请求往返的那几百毫秒，整个研究过程照样能拖——等于没修。**<br>正确门控在 `:425`：`const hasStream = streamStatus !== 'idle' || events.length > 0`，覆盖整个流生命周期；**并顺带覆盖刷新后的降级态**——`:395-397` 的 `setRun(loaded)` + `subscribe(threadId)` 会让 events 涨起来，`hasStream` 自然为真，无需单独处理。<br>**实施顺序（不可颠倒，P1 才是对的那个）**：**P1 根治**（分母只认本次 run 的 config）→ **P0 护栏**（`disabled={hasStream}`，防手抖与误导）→ **断言锁**（下面 AC2 增补）。P0 只是"别让用户改"，**P1 是"即使有人改了，分母也不受影响"**。<br>② **上界取"本次 run 的快照"而非 UI 当前值（P1，根治）**：`startResearch` 成功后把 `{ maxIterations: 本次发送值, maxVerifyAttempts: 2 }` 与 threadId 一同存进 `currentRun.ts`（复用 B3 的模块级单例），`resolveProgress(run)` **优先读快照**。读不到（刷新后 B3-1 接管）才退回 UI 值——**此时宁可不准，也不高于 0.95**（钳制兜住）。<br>**›› 快照形态必须注明（v1.12）**：`currentRun` 现有形态是 `{ threadId, question, startedAt, phaseLabel }`，**`maxIterations` 不在里面**。快照时要**一并加入**，并在类型旁注明 **「这是启动时的值，此后不变」**——否则后人看到"存了个 config"会以为是实时值，那 P1 就把跳变换了个位置重现。<br>**›› 验收怎么锁（AC2b 增补）**：运行期间拖动滑块 → **`fill` 必须完全不变**；反向用例：拖到 8 再拖回 3，断言与拖动前**逐帧相等**。**只验"与数值无关的性质"**（本条与具体百分比无关，符合 R17）。<br>· `maxIterations` 取 UI 滑块（`ResearchWorkflow.tsx:293`），已由 `schemas/graph.py:10` `max_iterations: Field(default=3, ge=1, le=8)` 反证为事实<br>· `MAX_VERIFY_ATTEMPTS = 2`，已由 `schemas/graph.py:11` `max_verify_attempts: Field(default=2, ge=1, le=4)` 反证为事实；`api/graph.ts:14` 的 `?? 2` 同源<br>· **默认配置 (3,2) 代入 = 3 + 8 + 2 = 13**；滑块拉满 (8,2) = 18<br><br>**›› 我原来那两处错（v1.6 更正，附推导，便于复核）**——按 `graph.py:45-51` 与 `:65-74` 的条件边走：<br>① **research 执行 `maxIterations` 次，不是 maxIterations − 1**：判定在节点跑完后才做（`iteration >= max_iterations` → `retrieve`），`iteration` 每执行 research 加 1（`nodes.py:255/267/301/348`），所以 research 恰好跑满 `maxIterations` 次。<br>② **一次回炉是 4 步，不是 1 步**：回炉回到 `research` 时 `iteration` 已超上限，下一决策点直接转 `retrieve`，因此每次回炉整串重跑 `research → retrieve → analyze → verify`；而回炉次数是 **`MAX_VERIFY_ATTEMPTS − 1`**（最后一次 verify 撞上 `verify_attempts >= max_verify_attempts` 才写报告，`:72-73`）。<br>默认 (3,2) 最坏路径：<br>`understand → plan → research×3 → retrieve → analyze → verify → research → retrieve → analyze → verify → write` = **13 步**。<br>**旧式在多数配置下反而会偏小（不只 (3,2)）**：(3,3) 旧 12 / 实 17、(8,2) 旧 16 / 实 18、(8,4) 旧 18 / 实 26 —— 这几组下进度条会**提前钉在 95% 走完全程**。旧式只在 (1,1)/(3,1)/(8,1) 偏大。<br>**›› 定性（同意 frontend-architect）**：这是**建模估计，不是权威总数**。它按「节点访问次数」建模；而 `steps` 由各节点自行 append，我**未逐节点核过 append 量**，二者可能有出入。真正的保证是 0.95 钳制，不是这个数。<br>· **钳制**：`fill = min(len(steps) / 上界, 0.95)`<br>· **接口留缝**：所有进度取值只从 `resolveProgress(run) → {numerator?, fill}` 出，将来后端给 `current_step` 只改这一个函数。<br>**›› 命名对齐（双向，照 frontend-architect §12.7）**：架构文档里 `computeRunUpperBound` / `done` 的取值来源**就是上面这个缝的内部实现**，二者**是同一个缝，不是两套**。施工员翻两份文档对得上——**本轮不要另起第二个函数去管进度取值**。<br>**›› 代码注释强制项**：注释须写明 ① `MAX_VERIFY_ATTEMPTS` 是**镜像后端默认值**；② 后端一改就**静默失准**；③ 吸收漂移的是 95% 钳制，不是这个常量。<br>**›› 施工禁令（ frontend-architect 的 R17，务必照做）**：**禁止拿这个建模值与后端日志里的实际步数做断言/比对测试。** 图结构一变，这类断言会**假失败**，而且**失败信息会把人引向错误方向**（以为是分母算错了，其实是图变了）——**比没有这条测试更糟**。要验证分母，只验证「`fill` 恒 ≤0.95 且随 steps 单调不减」这类**与具体数值无关的性质**。 |
| **A6** | **骨架屏用在正确的场景** | P0 | `LoadingState.tsx:24-45` 已实现但长等待页面反而没有骨架。A1 的 `.run-board` 占位块即为长等待骨架；`AgentRunner.tsx:130` 的 `variant="list"` 保持 |
| **A7** | **把无聊等待变成有产出等待** | P1 | `.run-board` 右侧显示 2 条建议（复用 `page__aside` 现有文案 `:544-550`），点击即跳 |
| **A8** | **顶部缓动指示** | P2 | 长任务期间 `nav-height`（`tokens.css:103`）52px 缓降到 46px + 顶栏 2px 进度指示条 |

### 5.2 操作反馈（对应 R1 / R5）

| ID | 需求 | 优先级 | 现状与改动 |
|---|---|---|---|
| **B1** | **按钮三态可辨** | **P0** | `disabled` 改为 `--surface-alt` 底 + `--line` 边 + `--faint` 字；新增 `.button--loading`（`aria-busy` + 脉冲圆点 + `cursor: progress`） |
| **B2** | **全局 Toast** | **P0** | 新增 `components/Toast.tsx`：**模块级单例 + `<ToastHost/>`**（复用 `useRunEvents.ts:26-28` 模式，零 prop drilling、不改 App 结构）。接入点：`KnowledgeBase.tsx:48/52`（**toast + 就地 `.alert` 双轨**，文件名需要被看见）、`ResearchWorkflow.tsx:316`（启动成功）、`:331`（批准 / 终止）、`:347`（刷新失败）、引用复制 |
| **B3** | **全局运行状态条 + 顶栏胶囊** | **P0** | 解决 D15。新增 `features/workflow/currentRun.ts` 模块级单例存 `{ threadId, question, startedAt, phaseLabel }`；① `.page__head` 下方状态条「● 研究中 ·「问题摘要」· 检索中 · 1:12 [查看进度]」；② 顶栏同名胶囊，<768 缩为纯脉冲点 |
| **B3-1** | **刷新后的降级方案** | **P0** | **问题**：`currentRun` 单例只活在内存，刷新即失效，此时顶栏显示"无运行中任务"而研究还在跑。<br>**后端现状**：`GET /api/graph/runs?status=running` 存在，但直接用踩三坑——① 漏掉 `awaiting_approval`（`service.py:87` 的 `TERMINAL_STATUSES` 之外）；② 返回全量 state 快照；③ **僵尸 run**：`InMemorySaver`（`graph.py:116`）进程重启即空，status 却已落 SQLite 无人改。<br>**本轮方案（纯前端，等价于专用端点）**：挂载时调一次 `listRuns(undefined, 3)`，取回候选 run 后**由谓词判定展示与标注**。<br>**›› B3 边界裁定（全局口径，v1.7 定稿）**：**陈旧判定的唯一判据是 `status === 'running'`。`awaiting_approval` 无条件豁免——这是数据丢失，不是体验问题**，一条停在「待确认」的报告从列表消失，用户**没有任何办法找回它**。<br>**›› 为什么不是"同一个状态的两种说法"**：`awaiting_approval` 不进陈旧判定，是因为**「待确认」与「可能已中断」是两个正交信号**——两者**成因不同**（前者用户在思考，后者 run 可能已死）、**输出位置不同**（前者常驻「待确认」角标，后者是「可能已中断」提示）、**处置动作也不同**（前者等用户点击，后者提示可终止）。两者共用同一个 `status` 事实，但用途不同，所以是**两个函数不是一个**。<br>**›› 谓词的正确写法（v1.7 整段替换 v1.5 那段）**：**两个正交谓词，命名即语义，不许合并成一个函数**——<br>`// currentRun.ts —— 两个正交谓词，命名即语义，不许合并成一个函数`<br>`/** 是否出现在运行列表里：待确认的 run 必须可见，否则用户没有找回它的办法 */`<br>`export function shouldListRun(run: ResearchRunSummary): boolean {`<br>`&nbsp;&nbsp;return run.status === 'running' || run.status === 'awaiting_approval'`<br>`}`<br>`/** 是否该挂「可能已中断」提示：只有 running 才会陈旧，这是时间推进的状态 */`<br>`export function isStaleRun(run: ResearchRunSummary): boolean {`<br>`&nbsp;&nbsp;if (run.status !== 'running') return false`<br>`&nbsp;&nbsp;return (run.idle_seconds ?? 0) > STALE_AFTER_SECONDS`<br>`}`<br>**命名说明**：`isRenderableRun` / `isStaleRun` 合并时代的写法已作废。本轮这两个函数都是**新建**（当前代码不存在，`isRenderableRun` 只是我 v1.5 规格里的示意名），所以**改名无迁移成本**——这里的「命名即语义」是**强制项**；与 A5 那个 `computeRunUpperBound` 不同（那里改名会打乱封闭名单，靠注释挡误用）。<br>**›› 白名单的判据（v1.8 补，比白名单本身重要）**：`awaiting_approval` 进白名单的理由是**「用户没有找回它的办法」**，不是「它比较特殊」。⇒ **将来新增任何状态时，判断顺序固定为：先问「如果它从列表消失，用户有没有办法找回」，答「没有」就必须进白名单。**<br>白名单会随状态增多而腐化（加一个漏一个，漏的那个静默消失），**判据不会**——按判据审新状态时，漏掉的那个会在评审时被问住。<br>``// 新增状态准入自查，加进 code review 清单`<br>`// 1. 这个状态如果从列表消失，用户有没有别的路径找回？`<br>`// 2. 答"没有" → 必须进 shouldListRun 白名单；答"有" → 不许进`<br>``<br>**›› 为什么不允许合并（原 v1.5 写法必须先拦下）**：v1.5 那段把两件事塞进一个返回布尔的函数里，唯一可行的写法就剩「加一行前置豁免」——`awaiting_approval` **靠豁免行的位置**才免于陈旧。**正确性绑在行序上，不绑在设计上**：任何人做一次「清理提前返回」重构，语义就静默反转，且反转方向恰好是把待确认报告从列表里弄丢。**不报错、不测试红，只丢数据。**<br>**›› 实现约束（v1.7 更新）**：那 5 处（状态条 / 列表 / 恢复区 / 状态胶囊 / AC8）是**展示点不是过滤点**；当前代码**不存在任何 180s 过滤**（全库无 `Date.now()` 陈旧判定；team-lead 已更正其"前端原本已有该规则"的说法）⇒ **B3-1 是本轮首次引入，属「新增判定」，不是"修正既有过滤"**。**只从这两个谓词出结果，绝不在 5 个组件里各写一遍。**<br>**›› 阈值取后端时钟，浏览器不重算（v1.7 重要更正）**：陈旧判定用 `run.idle_seconds`，**不用 `Date.now() - Date.parse(run.updated_at)`**。理由是权威时钟只有一个：浏览器时钟可能因用户改系统时间 / NTP 漂移 / 时区配置而偏，而阈值正是 180s 这个量级，**偏几百秒就会出现「一条活着的 run 被显示成已中断」**。<br>· 字段来源已核实为**本轮后端既有交付**，非我臆造：`_row_to_dict` 补 `idle_seconds`（`backend-refactor-architecture.md:1641` 的改动 `:1651-1664`，**返回的其他字段一个都不改**）；判据也明确要求用后端值（同文档 `:1699`）。<br>· **缺值兜底（必做）**：`run.idle_seconds` 缺位 / 非有限数 ⇒ **不挂「可能已中断」标记**。**宁可漏报，不可误报**——误报会把活着的 run 说成已中断，误导读用户终止。<br>· **⚠️ 排期依赖**：`idle_seconds` 由本轮后端改造产出。**若后端 AC-21c 未先落地，本谓词恒为 false（fail-open，安全方向），但 AC8b 无法验收。** 需 team-lead 确认两者落地顺序。<br>**›› 为什么 `isStaleRun` 是标注谓词、不是过滤谓词（v1.8 更正理由）**：不是"避免两套阈值漂移"（漂移可测，测一条断言就能钉住，不该因此决定架构）。真正的原因是——**过滤和标注对用户是两件事**。<br>· **过滤**：run 从列表消失，用户**没有任何办法处置它**（看不到、点不到、终止不了）。<br>· **标注**：run 留在列表里，挂一条「可能已中断」，用户**看得见它还在，并能终止它**。<br>若两边阈值相同，跨过阈值的那一瞬间 run 是被**过滤**掉的，**标注路径根本不会被执行**——用户看到的是"任务凭空消失"，而不是"任务被标记"。**这恰好是 B3 裁定最反对的那种失败。**<br>**⇒ 契约：前端标注阈值必须严格小于后端 SQL 截断值，中间的窗口是刻意留的。** 只有在这个窗口里，run 既没被过滤（还能看见）、又被标注了（知道有问题）——这是唯一能让用户「看见并可处置」的区间。**阈值相等或反过来，这条路径就死了。**<br>· **两个阈值都要进文档，且标出大小关系**：<br>| 阈值 | 作用域 | 当前值 | 状态 |<br>| `STALE_AFTER_SECONDS`（前端标注） | `isStaleRun()` 挂「可能已中断」 | **150s** | 本轮前端定 |<br>| `stale_after_seconds`（后端 SQL 截断） | `list_runs` 剔除陈旧 `running` 行 | **180.0** | 后端常量 `ZOMBIE_IDLE_SECONDS`（`backend-refactor-architecture.md:1592`，`:1607` 作默认参数）· **规划中** |<br>· **前端 150 = 后端 180 − 30**，30s 是**缓冲带宽**（覆盖一次往返与标注渲染），**不是体验调参值**——改它必须同时改下面那条断言。<br>· **可执行的断言（锁住这个关系）**：`expect(STALE_AFTER_SECONDS).toBeLessThan(BACKEND_STALE_AFTER_SECONDS)`。漂移会被它抓住。**后端常量当前不可得时该断言 skip 并打印原因，绝不静默通过**——与 AC8b 的「待验」口径一致。<br>· **状态码语义**：后端谓词同样只作用于 `status='running'`，`awaiting_approval` 走独立文案、不进 staleness（`backend-refactor-architecture.md:1690-1698`）。<br>**›› 后端陈旧过滤当前不存在（v1.8 更正，勿再引用）**：已核实 `backend/app/graph/run_store.py:92-103` 的 `list_runs` 是裸 SQL（`ORDER BY updated_at DESC LIMIT ?`，**无任何 cutoff 过滤**），且 `backend/app/` 内 `grep stale/STALE_AFTER/180` **零命中**。`backend-refactor-architecture.md:1606-1609` 那条谓词是**尚未落地的规划内容**，不是现状。⇒ 上表的「前后端阈值大小关系」目前是**对后端的约束**，不是对前端的描述。<br>**永不轮询**；同会话内永远以 `currentRun` + SSE 为准；本方案只在"刷新/新标签页"接管。 |
| **B4** | **错误必须给下一步** | **P0** | `ErrorState.tsx:20-37` 启用 `tips` 入参，全部 6 个调用点补上 |
| **B5** | **乐观更新** | P1 | 批准 / 终止立即改变 UI，失败回滚 + toast 说明 |
| **B6** | **终止可撤销** | P1 | `toast.info('已终止研究', {action:{label:'撤销'}})` → `resumeResearch(id, true)`；catch 回滚兜底为标准做法，后端不保证也照交付 |

### 5.3 焦虑与键盘（对应 R1）

| ID | 需求 | 优先级 | 说明 |
|---|---|---|---|
| **C1** | **统一焦点环** | **P0** | 全库补 `:focus-visible`（现状 **0 处**），复用设计令牌 |
| **C2** | **⌘K 命令面板** | P1 | 输入即筛，↑↓ 选择，Esc 关闭 |
| **C3** | **导航焦点管理** | P1 | 切页后焦点落到 `<h1>` 或 `.page__head` |
| **C4** | **Esc 分层关闭** | P1 | 抽屉 → 命令面板 → 模态，优先级队列 |
| **C5** | **快捷键可发现** | P1 | 在命令面板与「教程」列出全部快捷键 |
| **C6** | **vim 式跳转** | P2 | `g d` 等 |
| **C7** | **表单校验** | P0 | touched 感知（`useFieldHint`），提交前一次性提示 |
| **C8** | **输入防抖** | P1 | 搜索类输入 300ms 防抖 |

### 5.4 即时反馈

引用复制 →「已复制引用」toast；上传成功/失败 → 双轨。

---

## 6. 视觉细节打磨清单

| ID | 需求 | 优先级 | 说明 |
|---|---|---|---|
| **6.1 排版节奏** | 建立 `--space-1..10` 刻度，消除 3px/5px 等非令牌值 | P1 | 间距全部走令牌 |
| **6.2 深色主题** | 6 项专项修复 | **P0** | 白字对比、骨架（`--surface-alt` 于 `--page` 仅 1.2:1）、下沉容器、环境光、`--faint`、强调色（新 `--accent-strong` 专承载白字，因白字于 `--accent` 仅 2.4:1） |
| **6.3 过渡动效** | 统一 `--dur-*` / `--ease-*` | P1 | 去廉价动效（弹性/过度旋转） |
| **6.4 图标** | 统一 line SVG 图标集 | **P0** | 新增 `components/icons.tsx`，手写，不引依赖 |
| **6.5 留白** | 卡片/列表内边距对齐刻度 | P1 | |

---

## 7. 首屏与新用户引导

| ID | 需求 | 优先级 | 说明 |
|---|---|---|---|
| **H1** | 概览 hero 承载后端连接状态 | P1 | `Dashboard.tsx:109-116` |
| **H2** | 会话内记忆上次页面 | P1 | `sessionStorage['arw-page']`（**不用 localStorage**，避免新用户开屏看到陌生页）。`App.tsx:74` 初始化 + `useEffect` 写入 |
| **H3** | 上手进度条 | P1 | 首次访问（`localStorage['arw-onboarded']` 不存在）时 hero 下方出现 `① 跑通一次研究 → ② 传一份资料 → ③ 拿到报告`，已完成打勾，点击即跳 |
| **H4** | 教程同步导航地图 | P1 | 修正 `Tutorial.tsx:115`「五个模块」与实际 9 项 + 3 组不符 |

---

## 8. 用户故事

1. **As a 新用户**，我想打开就直接看到「3 步开始研究」，以便 30 秒内跑通第一次研究，而不需要先理解 9 个导航项。
2. **As 一个正在等研究的客户**，我想在任何页面都能看到「研究中」和剩余时间，以便我知道该等还是该做别的。
3. **As 一个刚被中断确认的用户**，我希望「待确认」提示**永远不消失**，以便我随时能回去找到那份报告。
4. **As 一个键盘用户**，我想用 ⌘K 直达任何页面并用 Esc 逐层退出，以便不摸鼠标也能用。
5. **As 一个提交失败的用户**，我想看到「为什么失败 + 下一步怎么改」，而不是一个灰掉的「重试」按钮。

---

## 9. 需求池

### P0（必须有）

| ID | 需求 | 章节 |
|---|---|---|
| P0-1 | 导航 IA 重组 + 三档视口 | §4 |
| P0-2 | 导航分组与焦点环 | C1 |
| P0-3 | 焦点管理 | C3 |
| P0-4 | 启动即出研究看板 | A1 |
| P0-5 | 分阶段可见 | A2 |
| P0-6 | 全局 Toast | B2 |
| P0-7 | 按钮三态 / 状态条 / 错误下一步 | B1 / B3 / B4 |
| **P0-7b** | **刷新后降级探测**（`listRuns` + `shouldListRun` 过滤，`isStaleRun` 标注，**永不轮询**） | B3-1 |
| P0-8 | 表单校验 | C7 |
| P0-9 | 骨架屏用在长等待 | A6 |
| P0-10 | 图标统一 | 6.4 |
| P0-11 | 深色主题 6 项修复 | 6.2 |
| P0-12 | `tips` 补齐 6 处调用点 | B4 |
| P0-13 | 去掉廉价动效 | 6.3 |

### P1（应该有）

导航 ⌘K、命令面板、Esc 分层、快捷键可发现、输入防抖、会话内页面记忆、上手进度条、ETA（A3）、历史事件预览前置（A4）、等待期建议（A7）、排版节奏、留白刻度、乐观更新（B5）、终止可撤销（B6）、教程同步导航（H4）、**ETA 口径小字（A3 展示处）**。

### P2（可以有）

顶部缓动（A8）、vim 跳转（C6）、`current_step` 精确进度（升级 `resolveProgress`）、ETA 精度提升（A3-2）、reduced-motion 全覆盖（W4）。

### 技术债登记（不施工）

见 **附录 C**。

---

## 10. 验收标准

| ID | 标准 | 验证方式 |
|---|---|---|
| AC1 | 导航重组后**任意功能页 ≤3 步到达** | 人工走查 9 页 × 3 档桌面视口 |
| AC2 | 启动研究后 **≤1 秒**出现研究看板；此后**任意 3 秒内界面有可见变化** | 秒表 + 录屏回放 |
| AC2b | 进度条**任何时刻不提前到 100%**：显示值恒为 `min(len(steps)/上界, 0.95)`，`上界 = maxIterations + 4×MAX_VERIFY_ATTEMPTS + 2`（**代码中不得出现硬编码步数常量**）；文案只给分子 | ① 跑满循环实测（`maxIterations=8`，上界应为 **18** 非 13）② 反例：`len(steps)` 超上界时断言停在 95% ③ 代码扫描无 `=== 11` / `=== 13` 类魔数 |
| AC2c | **进度条必须始终在动**（v1.6 新增，frontend-architect 提出）：默认配置跑满一次，**条不得出现"连续多秒不动"**。与「假满格」是**两种表现、同一个心理机制**——用户同样会把「条不动」读成「卡住了」。<br>本条正是 AC2b 的反面：AC2b 管的是**不许透支**（不许满），AC2c 管的是**不许装死**（不许停）。只守 AC2b 会放行"钉死在 95% 走完全程"这种实现。<br>**禁跳变（v1.11 增补）**：**不许"条因无关操作而动"**——运行期间拖动「研究最大循环轮数」滑块，`fill` 必须**完全不变**；反向用例：拖到 8 再拖回 3，断言与拖动前**逐帧相等**。<br>AC2b 原本只锁"不满 100%"，**锁不住"从 78% 跳到 95%"**。与 AC2b/AC2c 同源：**条动了而 run 没动，用户就会读成"这东西不可信"**。 | 录屏逐帧比对：默认 (3,2) 跑满一次，统计两次可见变化之间的最长间隔 + **拖动滑块前后 `fill` 逐帧比对** |
| AC3 | 四档视口（375/768/1024/1440）× 9 页 × 明暗双主题：**零横向溢出** | 浏览器遍历 |
| AC4 | 同上 × 双主题：**所有文本对比度 ≥4.5:1**，扫描可疑项 **= 0** | axe DevTools / 手动取样 |
| AC5 | 键盘可达：全部交互元素可 Tab 达且有可见焦点环；⌘K 与 Esc 分层正常 | 键盘遍历 |
| AC6 | 每个写操作都有 toast 或就地反馈，无"点了没反应" | 走查 |
| AC7 | 全部 6 处错误态带 `tips`，且文案说明下一步 | 逐页检查 |
| AC8（同会话，不变） | **同会话内**切页 ×3 次状态条 100% 准确且始终可见；切页返回自动恢复 | 切页 ×3 |
| **AC8a**（v1.7 从 AC8 拆出） | **可展示性**：`status='awaiting_approval'` 的 run **必须出现在运行列表中**，且常驻「待确认」角标。<br>**反向用例（不得省略）**：把该 run 摘掉后——用户**没有任何办法找回它**（报告在服务端，界面上不可达）。<br>判定口径：`shouldListRun()` 恒为 `running \|\| awaiting_approval`，**不含任何时间项**：展示侧的时间判定是陈旧侧的事，不是可展示性的事 | 构造 `status=awaiting_approval` 且 `idle_seconds` 极大 → **必须仍显示** |
| **AC8b**（v1.7 从 AC8 拆出，v1.8 改阈值口径） | **陈旧性**：只有 `status='running'` 且 `idle_seconds > STALE_AFTER_SECONDS`（**150**，非 180）才挂「可能已中断」。<br>**反向用例（不得省略）**：① `awaiting_approval` 且 `idle_seconds` 极大 → **不得**挂陈旧标记（挂了就是把"用户在思考"错说成"run 已死"）；② `running` 而 `idle_seconds < 150` → 不得挂。<br>缺值兜底：`idle_seconds` 缺位/非有限数 → **不挂**（宁可漏报） | 后端进程重启造僵尸 run + `awaiting_approval` 超阈值反例 + **缺值不挂**反例 |
| **AC8b-前置**（v1.8 新增，**后端约束**） | **前端标注阈值必须严格小于后端 SQL 截断值**——`STALE_AFTER_SECONDS(150) < stale_after_seconds(180.0)`，否则 run 一跨阈值就被后端过滤掉，**标注路径永不执行**，用户看到"任务凭空消失"。<br>该断言锁住这个关系：漂移会被抓住；**后端常量不可得时 skip 并打印原因，绝不静默通过**。<br>**⚠️ 当前前端 150 = 180 − 30，后端的 180 尚未落地**（已核实 `run_store.py:92-103` 无 cutoff），故本条目前是**对后端 AC-21c 的约束** | 单测 `expect(STALE_AFTER_SECONDS).toBeLessThan(BACKEND_STALE_AFTER_SECONDS)` |
| **AC8b-反向用例（后端 AC-21c）**（v1.8 新增） | `_row_to_dict` 新增字段时，**返回字段集合必须只多不少**。`state` 键是 `service.get_research()` 的 DB 回落读取入口（`record["state"]`），**删改任何一个字段都会静默打断一条生产路径**——不报错、不测试红。<br>用例：`set(_row_to_dict(row).keys()) == 改造前集合 | {'idle_seconds'}`（白名单式断言，将来再加字段必须同步改这里） | 单测（改造前后键集合比对） |
| AC8c（作废，保留备查） | v1.6 的 AC8 单条写法：把两条语义合进一条验收。**已拆为 AC8a / AC8b**——合着写会让 QA 只验到"不误杀"就放行"该标不标"，两条必须分开验收。（v1.8 补记：阈值口径 180 → 150 的原因见 AC8b-前置，不是改小了更严格，而是要让标注路径**有机会执行**） | — |
| AC9 | 图标统一，无重复实现 | 走查 |
| AC10 | 过渡动效统一走 `--dur-*`/`--ease-*` | 代码扫描 |
| AC11 | 首次访问到「看到已填好示例问题的研究界面」**≤4 次点击、≤30 秒** | 新用户模拟走查 |
| AC12 | 深色主题 6 项修复生效；`prefers-reduced-motion` 全覆盖 | 对比度扫描 + 系统开启减少动效遍历 |
| AC13 | 内联样式归零 | 代码扫描 `style={{` |

**AC-24（约定）**：任何验收断言改动时，**必须自带一条"为什么这么断言"的解释**。它约束的是将来改断言的人——写错的验收标准会被 QA 当"已实现"通过，一条错断言等于漏测一整片。

**AC8b 验收前置（v1.8，QA 必读）**：

> 后端 AC-21c（`list_runs` 返回 `idle_seconds`）落地前，**AC8b 不得判通过**。QA 在依赖未满足时给 AC8b 打 **「待验」**，而非「不通过」——两者的区别是：**前者会自己变绿**（后端一落地重跑就过），**后者会让下一个接手的人以为要改代码**。

依赖链是「**后端字段 → 前端判定 → 前端验收**」，中间断了谁都不算完成。T04 不受此阻塞：前端按 fail-open（缺值不挂标记，宁可漏报不可误报）开工，方向安全。

**AC8z（QA 判定口径，v1.8 新增）**：凡验收项依赖外部未落地交付时，QA 一律记 **「待验」**，禁止记「不通过」，也禁止记「通过」。理由同上——「不通过」会把一个"等上游"的条目变成"要改下游"的工单。

**AC8 的作用域（v1.9，QA 必读）**：本条只管前端契约——**陈旧 `running` run 若出现在列表里，必须挂「可能已中断」且保持可处置**（看得见、点得到、能终止）。<br>**「僵尸 run 会消失」不是前端的需求**：那是后端 `run_store.list_runs` 的 WHERE 谓词 + AC-21 的行为（`backend-refactor-architecture.md` §12.1.3），归后端 T09。<br>**⇒ 过渡窗口内陈旧 run 滞留列表，是正确态，不是缺陷**——后端 AC-21 落地后僵尸静默消失（有后端验收），落地前僵尸留在列表但**可见、可标注、可终止**。**这两段是同一个设计的两个阶段**，不是"缺了完成态"。

---

## 11. 待确认问题

| ID | 问题 | 默认决策 | 风险 |
|---|---|---|---|
| Q1 | `workflow` 导航改名？ | **「深度研究 ★推荐」**，只改 label 与 3 处 eyebrow | 极小 |
| Q2 | 组标签文案用「研究/结果/系统」还是「工作时间/洞察/系统」？ | **「研究 / 结果 / 系统」** | 小 |
| Q3 | 抽屉里放几项？ | **9 项全放 + 3 组标题 + 主入口置顶** | 小 |
| Q4 | 是否做「上手进度」三步引导？ | **做轻量版**，不做全屏向导 | 中 |
| Q5 | 引入全局 Toast？ | **引入**。实现限定模块级单例，零 prop drilling、不改 App 结构、不新增依赖 | 小 |
| Q6 | 命令面板 | **做**，⌘K | 中 |
| Q7 | ETA 数据来源？ | **零后端依赖**：`created_at` 后端本就在返回；删 localStorage 降级；**读不到就不渲染 ETA」（绝不渲染 `NaN`）** | 中 |
| ~~Q7b~~ | ~~请后端补 `created_at`？~~ | **v1.3 关闭：后端已返回，无需补。** 教训：**TS 类型声明 ≠ 后端实际返回**，凡"后端是否有某字段"一律回 schema / route 层核实 | 极小 |
| ~~Q8~~ | ~~「搜索用量」配额字段？~~ | **v1.4 关闭**：字段**已落地**，契约见 **§12**。但**本轮不做该 UI**，`buildGroups()` **本轮不加分组** ⚠️ `estimated_runs_remaining` 是 `number \| null` | 小 |
| Q9 | 刷新后「研究中」提示？ | `listRuns` + `shouldListRun` 过滤，永不轮询；`awaiting_approval` 豁免陈旧过滤。若后端后续交付 `GET /api/runs/active`（P1-7）直接替换 | 中 |
| Q10 | 深色主按钮：调暗 `--accent` 还是反色？ | **调暗**，新增 `--accent-strong` 专用于"承载白字的强调底" | 中 |
| Q11 | A5 分母口径 | 推导式上界 + 95% 钳制，见 A5 | 中 |
| ~~Q12~~ | ~~过渡窗口内，僵尸 run 由谁从列表里移除？~~ | **v1.9 结案，结论写死**：`shouldListRun` **不含任何时间项**，本轮**不可回退**。<br>**理由不是"时间属于陈旧侧不属于展示侧"**（那是结论不是理由）——是：**加了时间项，标注路径就死了，用户就失去了唯一的处置入口。**<br>**"冲突"的定性也错了**：AC8 里「僵尸 run 180s 内自动消失」**本来就不是前端的需求，是后端的**（AC-21 / 后端 T09）。前端不承担它不是"漏了"，是**不属于**。<br>**⇒ 谁负责改（不可违反）**：**过渡期内若发现陈旧 `running` run 仍滞留，这是后端 AC-21 未落地，归后端单。前端不得自行补过滤来"修"它**，否则 AC-21 落地后会出现两套过滤、两套阈值打架。<br>**⇒ 也不要写"临时态，AC-21 落地后删除"这类注释**——删除动作从来没发生过，但临时分支会一直活着并被后人当既定设计沿用。（已并入 §13.5） | — |
| ~~Q13~~（v1.8 关闭） | ~~「后端 SQL 的陈旧谓词是否可用？」~~ | **已核实不可用**：`backend/app/graph/run_store.py:92-103` 裸 SQL 无 cutoff，`backend/app/` 内 `stale / STALE_AFTER / 180` 零命中。属 AC-21c 规划内容，**不是现状**，以前的表述已删除 | 极小 |

---

## 12. 设置页「用量 / 搜索配额」组（后备需求 · **本轮不施工**）

> **状态说明（防止误施工）**：本节是**给将来做这个功能的人看的契约文档，不是本轮需求**。本轮 T01~T07 **不做该 UI、不预留占位、不写任何"等字段"的骨架**。`Settings.tsx:31-89` 的 `buildGroups()` **本轮不加「用量」分组**。

### 12.1 后端契约（`GET /api/settings` 的 usage）

字段名**照抄，不要改名**（`backend/app/search/quota.py` 的 `QuotaSnapshot`，该文件注释即此契约）：

| 字段 | 类型 | 说明 |
|---|---|---|
| `provider` | `string` | 默认 `"tavily"` |
| `period_key` | `string` | `'2026-09'` |
| `period_start` / `period_end` / `renews_at` | `string` | ISO，含 `+08:00` |
| `credits_limit` / `credits_used` / `credits_remaining` | `number` | |
| `ratio` | `number` | 0~1，四位小数 |
| `warn_level` | `number` | 0 正常 / 1 ≥50% / 2 ≥75% / 3 ≥90% / 4 耗尽 |
| `calls_total` | `number` | |
| `search_depth_default` | `string` | 默认 `"advanced"` |
| `estimated_runs_remaining` | **`number \| null`** | ⚠️ **可为 `null`**（近 7 天无 run 时） |
| `daily` | `Array<{date, calls, credits}>` | 已补零合并区段 |

TS 必写 `estimated_runs_remaining: number | null`。写成 `number` 会在渲染时踩 `null.toFixed(0)` 直接崩。

### 12.2 计费口径（UI 文案必须如实体现）

| 口径 | 事实 |
|---|---|
| 计费单位 | `basic` = **1 credit/次**，`advanced` = **2 credits/次** |
| 当前默认 | **`advanced`** |
| 后果 | **1000 credits 免费额度实际只够约 500 次搜索**——用户按 1000 次理解会高估一倍 |
| 重置周期 | **每月 1 日 00:00（+08:00）**，**不是滚动 30 天** |

**产品要求**：只要展示 `credits_remaining`，就必须同时说明「= f 次搜索」，**不得只显示 credits 数**——用户心智单位是"搜索次数"，而两者因 `advanced=2` 差一倍。

---

## 附录 A：7 个构建阶段

| 阶段 | 内容 | 可独立验收 |
|---|---|---|
| S1 | 导航 IA 重组 + 三档视口 + 焦点环 | AC1、AC5 前半 |
| S2 | 深色主题 6 项 + reduced-motion 全覆盖 | AC4、AC12 |
| S3 | 图标统一 + 去廉价动效 + 间距刻度化 | AC9、AC10、AC13 |
| S4 | 等待反馈：研究看板 + 分阶段 + 状态条 | AC2、AC2b、AC8 |
| S5 | 操作反馈：Toast + 按钮三态 + 错误下一步 | AC6、AC7 |
| S6 | 键盘与命令面板 | AC5 |
| S7 | 首屏引导 + 表单校验 + 防抖 | AC11 |

## 附录 B：新增/修改文件清单

| 类型 | 文件 | 说明 |
|---|---|---|
| 新增 | `src/components/Toast.tsx` | 模块级单例 toast |
| 新增 | `src/components/useEscape.ts` | 统一 Esc 处理 |
| 新增 | `src/components/useFieldHint.ts` | touched 感知表单提示 |
| 新增 | `src/components/EtaEstimate.tsx` | 预计剩余时间（纯展示） |
| 新增 | `src/components/Citation.tsx` | 引用来源去重 + 复制 |
| 新增 | `src/components/icons.tsx` | 手写 line SVG 图标集 |
| 新增 | `src/features/workflow/currentRun.ts` | 当前运行单例；**v1.7 起同时承载 `shouldListRun` / `isStaleRun` 两个正交谓词**（B3-1） |
| 修改 | `src/App.tsx` | 导航分组、⌘K、状态条、焦点管理 |
| 修改 | `src/styles/{tokens,base,layout,components,pages,responsive}.css` | 令牌、焦点环、动效、深色、间距 |
| 修改 | `src/features/**/*.tsx` | 内联样式清理、表单校验、引导、状态条 |
| 修改 | `src/api/graph.ts` | 新增 `fetchActiveRun()`（B3-1 降级探测，永不轮询） |
| 修改 | `src/types/graph.ts` | `ResearchRunSummary` 补 `created_at: string`（后端已返回，非可选）；**补 `idle_seconds: number`（v1.7；后端 `:1660` 补出，非可选；缺值走 `?? 0` 兜底不挂标记）**；`steps` 仍不声明 |

## 附录 C：技术债登记（**登记，不施工**）

> 只记录"知道它存在、但现在不动"的事。**每条都带触发条件**——条件不满足时不许顺手做，没有消费方的时候提前加列是纯开销。

### C-1 纯后端执行时长（`finished_at` 列）

- **是什么**：`updated_at - created_at` 现在是**总墙钟时间**（含人工确认等待）。若将来需要**纯后端执行时长**，唯一干净的解是给 `agent_runs` 加一列 `finished_at`（或 `confirmed_at`）——`updated_at - finished_at` 即人在确认页的停留时长，减掉即可。
- **本轮不做**，消费方为零。
- **触发条件**：**出现需要「纯后端执行时长」的场景时** —— （a）产品出现「跳过确认」开关，使确认等待不再必然发生、口径失去一致性；（b）需要用它做性能分析 / 回归对比。
- **为什么现在不做**：无消费方时加列是纯开销；且口径一变，`EtaEstimate`、历史列表、报表三处都要跟着改。

### C-2 「本月用量 / 剩余次数」文案（§12 施工时）

- **是什么**：§12「用量 / 搜索配额」组本轮不施工，其文案无所有人。team-lead 已在 §11 Q8 给出**判据（不是三选一，是分情况）**：
  | 情形 | 显示 |
  |---|---|
  | 配额关闭（`search_quota_enabled=false`） | **整块隐藏**，连文案都不显示 |
  | 配额开启、`credits_limit` 未知 | 「本月用量 x / —」，**不显估算次数** |
  | 配额开启、`credits_limit` 已知且为 0 | 「本月可用 0 次，已用完」 |
- **触发条件**：**§12 施工时**（即后端把配额契约正式交付、该组进入排期）。
- **现在不写死的理由**：为一个还没有界面的东西提前定死措辞，等于把一次可以推迟的设计决策变成既成事实。
- **但有一条禁忌要现在记下**：`credits_limit == 0` 时**不得写「剩余 0 次」**——「剩余」隐含还有一个存量在那儿，「可用 0 次」才是对的。这属于**判据层面**（语义错了），不是措辞偏好，所以提前记；具体文案留到触发时定。
- **另有一条跨角色坑**：`NaN != null` 是 `true`，配额脏值过不了 null 检查（架构文档 §11 Q8 已记）。

---

## 13. 方法论留档（本轮踩过的坑，写给下一个人）

> 这一节记的是**判断规则**，不是结论。结论会过时，判断规则不会。

### 13.1 推断出来的数字，一律先当待核项

本轮我在 A5 上界公式上连错两次（v1.2 硬编码 `11`、v1.5 推导式），两次的根因相同：**一个数在我写下它的时候，没有一条取证的是我**。

> **错误的特点不是它明显，是它长得像答案。** —— frontend-architect

那个错位的 `11` 恰好满足三条：是需求方给的（有出处感）、看着有依据（能自洽推导）、还能凑出一个整数。**三样都符合"答案"的样子，唯独不对。**

**规则（写进施工要求）**：
1. 任何进入需求的数字，必须能指到 `文件:行号`；指不到就写「推断，待核」，不许写成确定值。
2. 反证优于引用——`max_iterations default=3 / le=8` 之所以能当事实用，是因为 `schemas/graph.py:10-11` 是**约束**，不只是默认值。
3. 拿不准就**只写性质不写数值**（如 A5 只显分子、上界仅用于钳制），把数值风险关在钳制里。
4. **魔法数本身就是坏信号**：v1.2 的 `11` 被修成推导式是对的，但 v1.5 的推导式错了——**换掉魔法数不等于得到正确答案**。

### 13.2 验收标准必须能挡住"错误的实现"

- 只写「进度条不超过 95%」会放行「钉死在 95% 走完全程」——**两种表现、同一个心理机制**都读成"卡住了"。故 AC2b（不许透支）与 AC2c（不许装死）各管一头（v1.6 新增 AC2c）。
- **验收标准写错的危害大于没写**：B3-1 那次我把错误行为写进了验收标准，"错的验收会放过错的需求"。凡改验收，必须同时补**反向用例**。
- 断言要验**与数值无关的性质**（`fill ≤ 0.95`、与 steps 单调不减），不要拿建模值对后端日志做断言——图一变就假失败，且失败信息把人引向错误方向，比没有测试更糟。

### 13.3 交叉文档的一致性要靠"双侧同步"

- 两份文档各写一侧，各自看都对，但施工员会以为要管两个东西。**命名、接口这类跨文档契约，必须两边同写**。
- 只写「是同一个缝」不够：那挡住的是"误以为要管两个"，挡不住"照着再造一个"。**禁令要写在两侧同一条措辞里**。
- **变更记录不要把事件挂到被纠正的那一侧**——会让下一个人以为是自己做错了。现在 v1.6 第 ⑥ 条即按此处理（记为"双侧同步，非任一方的更正"）。

### 13.4 动手前先验证前提（本轮第二严重的错误）

`io.open(..., 'w')` 会在**求值参数前**就把文件截断为 0。我在没有备份、且 `docs/` 不在 git 管理下（`git ls-files docs/` 为空）的情况下跑了一个一次性脚本，把整份 PRD 清成空文件，最后靠逐条复核会话结论重建。

- **教训**：对**正在被消费的文档**跑任何一次性脚本前，先确认它在版本控制下或已有备份。**截断优先于异常**，错误信息不能救回已写入的内容。
- **补救动作已固化**：每次改完 PRD 都重跑一次完整性核对（行数 / 字节数 / 版本号行数 / 关键旧文本是否残留）。

### 13.5 跨层职责：不要替上游补位，也不要写"落地后删除"

- **① 跨文档「谁是权威」要逐个数定，不能默认某一侧**（v1.10 补，比下面的职责判断更靠前）。同一个数的权威来源**可以因数而异**：有的数「前端定义、后端引用」，有的数「后端定义、前端引用」——**只要逐个数写清「本数由谁定义、另一处只是引用」，就不是漂移**；**写不清的那一个，才是真漂移**。<br>⇒ 这次的洞不在"副本会漂"，而在**权威归属本身没有一把统一的尺子**：frontend 文档是阶段表的权威来源，而后端文档是同一批数据的权威来源，同一个「数」的权威来源却分在两份文档里。**不写这条，下次就会出现「副本删了，引用关系没留下」**——后人翻到那一节会以为那侧自己发明了这个数。<br>**⇒ 落法**：每处跨文档引用旁标注「来源：<文件:行号> · 定义方：<谁>」，读者一眼看出哪边是源头。
- **② 「谁的工作」问完之后还有第三种答案：谁都不属于**（v1.10 补，是第 ③ 条的 companion，否则会被用来躲问题）。答「谁都不属于」＝**真实缺口**，**不是**范围没划清。处理方式是**登记成带未来所有者的债**（写清触发条件与未来 owner），**不许静默跳过**。<br>⇒ 这不是假设：某处持久化定义的位置曾经是**空的，没有人拥有它**。而"没人拥有 = 没人更新"——下一次结构变更时**没有人会想起它**。<br>**「这不该我管」和「这目前没人管」是两件事**：前者是职责判断，后者是缺口。**只防前者，就会让后者一路滑到出事。**
- **③ 先分清一件事情归谁**：「僵尸 run 从列表消失」是**后端** AC-21 的职责（SQL 谓词），不是前端的。我一度把这条当成前端缺陷（"两边都没做导致验收落空"），实际是**AC8 把一件后端的事写进了自己的范围**。⇒ **提方案前先问"这是谁的工作"**，答不上来就说明范围没划清，别急着给方案。
- **④ 替上游补过滤 = 制造双重实现**。我提议的「过渡期前端临时承担过滤 + 注释『AC-21c 落地后删除』」被否，理由有二：① 后端一落地就是两套过滤、两套阈值；② **这类"落地后删除"的注释从来不会被删除**，但临时分支会一直活着，还会被后人当成既定设计沿用。**删除动作不发生，代码会替它存活。**
- **⑤ 更隐蔽的一层**：临时过滤会**直接废掉刚做对的那个标注**——`isStaleRun` 的全部意义是「run 留在列表里，用户能看见、能终止」。加了时间项把它过滤掉，**标注路径永远不执行**，用户只看到任务凭空消失。**检查"补位"动作时，要看它有没有把同链路上的正确路径一起掐掉。** 这条已在 v1.9 当天被实际验证过一次（跨文档权威归属的对称性缺口），见上①。

### 13.6 不变量不能只挂在"恰好在那儿"的东西上（判护栏，不是判 bug）

- **与 frontend-architect 那条判据互为镜像**：他的判兜底——「兜底分支会不会因此**永不执行**」（执行不到 = 实现过头）；本条判护栏——「**护栏被拆掉之后会不会有人知道**」（不会 = 护栏是装饰）。
- **合起来是一条**：**不变量不能只挂在一个"恰好在那儿"的东西上，无论那个东西是兜底分支还是 UI prop。它要么进类型、要么进断言、要么挂在一个会响的东西上。否则它只是长得像护栏。**
- **本轮实例**：我第一版把 P0 写成 `disabled={running}`，理由是"护栏总得有个挂点"。但 `running` 只覆盖 API 往返、不覆盖 SSE 生命周期——**护栏挂错了东西，且没有任何东西会因此报警**：拖滑块照样跳变，CI 照样绿。
- **用法**：写完任何"防手抖 / 防误用"的护栏后自问——**把它删掉，谁会知道？** 答不上来就别写它，直接去做不变量移位（让错误结果在结构上不可能），护栏只作为附带第二道。

---

## 变更记录

| 版本 | 变更 | 触发 |
|---|---|---|
| **v1.12** | ① **A5 的 P0 门控由 `disabled={running}` 更正为 `disabled={hasStream}`**（team-lead 查证：`:326/341/353/359/366` 的 `running` 只覆盖 `startResearch()` 那一次 API 往返，真正的运行靠 SSE 持续 1~2 分钟，那时它已是 `false`——**只挡几百毫秒，等于没修**）。`hasStream`（`:425` = `streamStatus !== 'idle' \|\| events.length > 0`）覆盖整个流生命周期，**并顺带覆盖刷新后的降级态**（`:395-397` 恢复会让 events 涨起来） |
② **明确实施顺序不可颠倒**：**P1 根治（分母只认本次 run 的 config）→ P0 护栏（`disabled={hasStream}`）→ 断言锁**。P0 是"别让用户改"，P1 是"即使有人改也不影响分母" |
③ **P1 的快照形态补注**：`currentRun` 现有 `{threadId, question, startedAt, phaseLabel}` 中**没有 `maxIterations`**，快照时要一并加入并注明**「启动时的值，此后不变」**——否则后人会当成实时值，P1 等于把跳变换个位置重现 |
④ **新增 §13.6（判护栏）**：与 frontend-architect 那条判据互为镜像——「兜底分支会不会因此永不执行」（判过头）/「**护栏被拆掉之后会不会有人知道**」（判装饰）。合起来：**不变量不能只挂在"恰好在那儿"的东西上，要么进类型、要么进断言、要么挂在一个会响的东西上** |
⑤ **新增附录 C-2（配额文案）**：登记 team-lead §11 Q8 的**分情况判据**（隐藏 / 不显估算 / 可用 0 次）+ 一条禁忌（`credits_limit==0` 不得写「剩余 0 次」，「剩余」隐含存量，属语义不属措辞）；**文案本身留到 §12 施工时再定**，触发条件已写清 |
| **v1.11** | ① **A5 / AC2b 新增「不许跳变」**（frontend-architect 复审 §12.5 自我更正后提出）。**AC2b 只锁"不满 100%"，锁不住"从 78% 跳到 95%"**：上界与滑块成反比，拖动即跳（向上拖**进度倒退** 38.5%→27.8%，向下拖**虚假跃进** 77.8%→95%）——**条动了而 run 没动**，与 AC2b/AC2c 同源<br>② **定性为 A5 留缝没做完整**（不是算错）：`resolveProgress(run)` 的**分子来自 run、分母却来自 UI 当前值**，而 run 的真实配置在 `:341` 启动时就已快照 ⇒ **用描述不了这个 run 的数去算它**<br>③ **两道修，职责不同都要做**：**`disabled={running}`**（P0，一行消成因——run 期间滑块本就无任何作用，能拖是在骗用户）+ **上界取本次 run 快照而非 UI 当前值**（P1，断根，刷新读不到时退回 UI 值并由 0.95 钳制兜底）<br>④ **AC2 增补禁跳变断言 + 反向用例**（拖到 8 再拖回 3，逐帧相等），只验与数值无关的性质 | frontend-architect §12.5 自我更正 |
| **v1.10** | ① **§13.5 补两条**，且排在「谁的工作」之前：**① 跨文档「谁是权威」要逐个数定**——同一个数的权威来源可以因数而异（有的前端定有的后端定），**写清「本数由谁定义、另一处只是引用」就不是漂移，写不清的那个才是真漂移**；**落法**：每处跨文档引用旁标注「来源：<文件:行号> · 定义方：<谁>」<br>② 补第 ③ 条的 companion：**「谁都不属于」＝真实缺口，不是范围没划清**——登记成带未来所有者的债（写清触发条件），**不许静默跳过**。「这不该我管」和「这目前没人管」是两件事，前者防得住，后者一路滑到出事<br>③ **A2 的「7 节点」改为推导来源**（按 team-lead 核定）：`graph.py:80-87` 的 `add_node` 顺序是唯一真相来源，**`STAGE_ORDER` 不存在且 `backend/tests/` 零命中——没有任何测试守着这个对应关系**；前端只引用不存副本，**禁止手抄第二份阶段清单**；后端按约束从图上推导 + 补断言 | team-lead 指出权威归属缺一把尺子 + 阶段清单断源 |
| **v1.9** | ① **Q12 结案，我提的「过渡期前端临时过滤」被否**（team-lead）。我把它定性成"缺陷"，实际是 **AC8 把一件后端的事写进了自己的范围**——「僵尸 run 会消失」归后端 AC-21 / T09，前端不承担它不是漏了而是不属于 ⇒ **"临时分支 + 落地后删除注释"这个方案本身就有毒：删除动作从不发生，代码会替它存活，还会被后人当既定设计沿用**<br>② **写入不可回退的约束**：`shouldListRun` **不含任何时间项**。理由不是"时间属于陈旧侧"（那是结论不是理由），是**加了时间项标注路径就死了，用户失去唯一处置入口**<br>③ **AC8 改写作用域**（不删）：前端契约 = 陈旧 `running` run 若出现在列表里**必须挂标记且保持可处置**；「消失」是后端行为，**不出现在前端验收里**。补「谁负责改」规则：**滞留归后端单，前端不得自行补过滤**（否则 AC-21 落地后双重过滤 + 阈值打架）<br>④ 新增 **§13.5 跨层职责**：不替上游补位 / 别写"落地后删除" / **补位动作要先看有没有把同链路的正确路径一起掐掉** | team-lead 否掉 Q12 并更正定性 |
| **v1.8** | ① **B3-1 谓词的定性对，但理由错了，已换**（team-lead：漂移可测、不该因此决定架构）。真实理由是**过滤与标注对用户是两件事**——过滤让用户无法处置，标注让用户能看见并能终止；**两边阈值相同则跨阈值的瞬间 run 是被过滤掉的，标注路径根本不会执行**，用户看到"任务凭空消失"。⇒ **契约：前端标注阈值严格小于后端 SQL 截断值，中间窗口是刻意留的**；加断言锁 `STALE_AFTER_SECONDS < stale_after_seconds`；前端定 **150 = 180 − 30**<br>② **删除"后端 cutoff 已存在"的错误表述**（已复核：`run_store.py:92-103` 裸 SQL 无 cutoff，`backend/app/` 内 `stale/STALE_AFTER/180` 零命中；`ZOMBIE_IDLE_SECONDS = 180.0` 是架构文档 `:1592` 的**规划值**）⇒ 该大小关系目前是**对后端的约束**，不是对前端的描述<br>③ **AC8b 阈值 180 → 150**（非"更严格"，而是让标注路径有机会执行）；**AC8b 验收前置**写进验收区：后端 AC-21c 未落地 → QA 记**「待验」**不记「不通过」（前者会自己变绿，后者会误导下一个接手的人）；新增 AC8z 通用判定口径<br>④ **新增两条后端约束**：AC8b-前置（阈值大小关系断言）+ AC8b-反向用例（`_row_to_dict` 字段集合**只多不少**，`state` 是 `get_research` DB 回落的读取入口，删改会静默打断生产路径）<br>⑤ **`shouldListRun` 补判据而非白名单**：新增状态先问「如果它从列表消失，用户有没有办法找回」——白名单会腐化，判据不会<br>⑥ 新增 **Q12**（过渡窗口内僵尸 run 由谁移除）+ 关闭 **Q13** | team-lead 批 ① 但更正 ② 的理由 + 补阈值契约 |
| **v1.7** | ① **B3-1 谓词从"一个函数"改为"两个正交谓词"**（team-lead 指出我 v1.5 那段把可展示性与陈旧性塞进同一个返回布尔的函数，唯一写法只剩"加一行前置豁免"）：`shouldListRun()`（展示）与 `isStaleRun()`（标注）分离，**命名即语义，不许合并**。原写法**正确性绑在行序上**——一次"清理提前返回"的重构就会让 `awaiting_approval` 从列表里静默消失，**不报错、不测试红、只丢数据**<br>② 补「正交信号」论证：待确认 / 可能已中断**成因、输出位置、处置动作都不同**，不是同一状态的两种说法<br>③ **陈旧判据由 `Date.now() - Date.parse(updated_at)` 改为 `run.idle_seconds`**（浏览器不做二次计算：时钟偏几百秒就会把活着的 run 显示成已中断）。字段已核实为本轮后端既有交付（`backend-refactor-architecture.md:1641/1651-1664/1699`），非臆造；缺值 ⇒ **不挂标记（宁可漏报不可误报）**；**标注谓词不是过滤谓词**，列表侧过滤由后端 SQL 承担，前端不再过滤一遍<br>④ **AC8 拆为 AC8a（可展示性）/ AC8b（陈旧性）**，各带反向用例；AC8c 作废留档。按 §13.2「改验收必补反向用例」，先对自己用<br>⑤ 新增**排期依赖提示**：`idle_seconds` 由后端 AC-21c 产出，未落地前 AC8b 不可验收（失败方向是 fail-open，安全） | team-lead 指出谓词合并制造缺陷 + 口径更正 |
| **v1.6** | ① **A5 上界公式更正（我 v1.2/v1.5 的公式是错的）——由 frontend-architect 逐条核对 `graph.py` 条件边发现，我复核确认**。旧式 `7 + (maxIterations−1) + MAX_VERIFY_ATTEMPTS` 有两处错：research 实际跑 **maxIterations** 次（判定在节点后，`nodes.py:255/267/301/348` 每执行加 1）；一次 verify 回炉是 **4 步**（`research→retrieve→analyze→verify`），且回炉次数为 `MAX_VERIFY_ATTEMPTS − 1`。**新式 `上界 = maxIterations + 4×MAX_VERIFY_ATTEMPTS + 2`，默认 (3,2) = 13（旧 11）**<br>② 补充：旧式**不只 (3,2) 偏小**——(3,3) 旧 12 / 实 17、(8,2) 旧 16 / 实 18、(8,4) 旧 18 / 实 26，这几组下条会提前钉死；旧式只在 (1,1)/(3,1)/(8,1) 偏大，方向并不一致（**「宁大勿小」的说法不成立**，32 组中偏大 8、偏小 24）<br>③ **新增 AC2c「进度条必须始终在动」**：「假满格」与「不动」是两种表现、同一个心理机制，都读成"卡住了"。只守 AC2b 会放行"钉死在 95% 走完全程"。AC2b 管不许透支、AC2c 管不许装死，各管一头<br>④ 确认定性：上界是**建模估计，不是权威总数**（按节点访问次数建模，`steps` 的实际 append 量未逐节点核实），真正的保证是 0.95 钳制；**A5 标题旁加固定标注**（照 frontend-architect 措辞：「非后端权威总数，亦不构成上界保证」），三个「非/不」缺一不可，防止"拿它当真相"或"拿它当安全上界"两种误用<br>⑤ **新增施工禁令（frontend-architect R17）**：**禁止拿上界与后端日志实际步数做断言**——图结构一变会**假失败**，且失败信息**把人引向错误方向**（误以为分母算错，实为图变），比没有测试更糟。要验证分母只验**与数值无关的性质**（`fill ≤ 0.95` 且与 steps 单调不减）<br>⑥ **补双向命名对齐**：明确 `computeRunUpperBound` / `done` 的取值来源**就是 `resolveProgress(run)` 的内部实现，同一个缝不是两套**，**本轮不许另起第二个函数管进度取值**。两侧的禁令措辞已一致（仅写「是同一个缝」只挡住"误以为要管两个函数"，挡不住"照着再造一个"）。本条属**双侧同步**，非任一方的更正——双向各写一侧会让施工员误以为要管两个函数 | 与 frontend-architect §12.7 同步<br>⑦ 新增 **§13 方法论留档**（四条判断规则：推断数字先当待核项 / 验收须能挡住错误实现 / 跨文档契约双侧同步 / 动笔前先验证前提）。记的是判断规则不是结论——结论会过时 | 本轮四轮评审的纠错沉淀 |
| **v1.5** | ① **A3-1 ④ 重定性：从"精度缺陷"改为"口径没说清楚"**（team-lead 核实 `graph.py:115-118` 的 `interrupt_before=["write"]` 无条件生效，**每一轮研究都要等人确认**，故不存在"部分 run 污染样本"）。结论：**算法没错，是总墙钟口径从未对用户说明** ⇒ 修法定为 P1 文案口径（ETA 附小字「含报告确认时间」），不改算法。补记 P2 观察：run 已停在确认页时"还剩 3 分钟（含确认时间）"是新别扭<br>② **附录 C 新增技术债登记**：C-1 `finished_at` 列，只登记不施工，触发条件「出现需要『纯后端执行时长』的场景时」<br>③ **拦下一处会静默回退的写法**：team-lead 提议判定收紧为 `if (run.status !== 'running') return false`，但该式会把 `awaiting_approval` 一并 `return false`，**恰好重新屏蔽 B3 裁定要保住的待确认信号**。收紧思路对，只需把豁免行前置；已在 B3-1 给出正确的双行写法<br>④ B3-1 措辞由「修正既有过滤」改为「**新增判定**」 | team-lead 口径重定性 + 谓词写法更正 |
| **v1.4** | ① **B3 边界裁定入库**：`awaiting_approval` **无条件豁免**陈旧过滤，定性为「数据丢失而非体验问题」。补强实现约束——**陈旧过滤必须只存在于单一谓词**，5 个展示点只消费结果；已核实**当前代码不存在任何 180s 过滤**，B3-1 是本轮首次引入，故不是"改 5 处"而是"写 1 处、复用 5 处"<br>② **A3-1 第④条：确认等待污染样本**——只按终态 `completed` 过滤筛不掉经过 `awaiting_approval` 的 run，其 `updated_at` 含人工确认等待；前端无法识别剔除；误差单向偏高、有界（中位数压制）<br>③ **按 team-lead 要求写清因果链**：僵尸过滤是 A3 精度的**前置依赖**——撤掉它 ETA 会**静默恶化**（界面无任何异常）<br>④ **§12 新增「设置页用量/配额」后备需求章节**（本轮不施工）：`QuotaSnapshot` 全字段表 + 计费口径（`advanced=2 credits/次`，1000 credits 实为约 500 次）+ 月度 1 日重置；⚠️ `estimated_runs_remaining` 是 `number \| null`；Q8 关闭，`buildGroups()` 本轮不加分组 | team-lead B3 边界裁定 + 配额字段清单 |
| **v1.3** | ① **A3 / Q7b / P1-13c：`created_at` 谜案解开——我此前的判断是错的。** `_row_to_dict`（`run_store.py:105-116`）本就返回 `created_at`；我补验的关键一环是 `routes/graph.py:87` 的 `response_model=ApiResponse[list[dict]]`（裸 dict、未经 Pydantic 裁剪），故字段直达前端。**结论：ETA 零后端可得，删掉 localStorage fallback 与"首屏空白 5 秒后突然出现 ETA"降级分支。**<br>② 隐患：该字段能到前端**纯属该端点没有正式 schema**，将来补上会在**无任何报错**下静默消失 ⇒ 新增取值兜底：缺失/非有限值时**不渲染 ETA**<br>③ **修正我自己的规格错误：陈旧过滤不得套 `awaiting_approval`**。判据只能是 `status==='running'`；改 B3-1 / P0-7b / Q9 / AC8 / 备用契约 5 处，AC8 补**反向用例**<br>④ A5 分母两项取值**已从推断升为事实**（`schemas/graph.py:10-11`）；新增代码注释强制项 + `resolveProgress()` 单一出口<br>⑤ 方法论留档：**TS 类型声明 ≠ 后端实际返回** | team-lead 提供 created_at 线索 + awaiting_approval 反例 |
| **v1.2** | ① **A5 分母去魔法数**：v1.1 的硬编码上界 `11` 只是把坑换个位置重现。改为**推导式**`上界 = 7 + (maxIterations − 1) + MAX_VERIFY_ATTEMPTS`；默认代入恰为 11，滑块拉满 8 时自动变 16<br>② **进度条硬约束**：`fill = min(len(steps)/上界, 0.95)`，永不高过 95%<br>③ Q7b 降级路径确认<br>④ Q8 字段不臆造<br>⑤ backend P1-7 / P1-8 退回 P1 | team-lead 裁决 + 进度条数学复核 |
| **v1.1** | ① A3 ETA 数据源改为「后端历史样本 + localStorage fallback」，新增 A3-1 样本口径约束（只取 completed / 按问题长度分档 / 中位数）<br>② **修正 A5 分母错误**：原按固定 8 阶段做分母不可靠（`research` 循环 + `verify` 回炉，实际 6~11），改为只显分子 + 安全上界<br>③ B3-1 降级探测方案（永不轮询）<br>④ B3-2 状态条文案诚实性<br>⑤ Q8 / Q9 修正<br>⑥ 验收新增 AC2b、改写 AC8 | backend-pm 核实后端能力后反馈 |
| **v1.0** | 初版 | — |
