# AI Research Workspace · UI 优化 PRD

| 项目信息 | 内容 |
| --- | --- |
| Language | 中文 |
| Project Name | `ai_research_workspace_ui_redesign` |
| 技术形态 | React 18 + Vite 5 + TypeScript，**纯手写 CSS**（无组件库） |
| 涉及范围 | `frontend/src/App.tsx`、`frontend/src/features/**`（11 个 tsx）、`frontend/src/components/**`（6 个 tsx）、`frontend/src/styles/index.css`（2951 行） |
| 交付物 | 产品定义（本文档）。**不写代码。** |
| 版本 | v1.0 / 简单 PRD |

## 原始需求复述

对已跑通的 AI 研究工作台前端做一次 UI 优化，共 6 条：

1. **R1 统一视觉规范**：配色、字号层级、圆角、阴影、间距与栅格对齐，各页面风格一致。
2. **R2 尺寸与分区**：调整容器与卡片尺寸，避免过大留白或元素挤压；内容按信息层级分区，区块间距与对齐整齐，杜绝杂乱堆叠。
3. **R3 可读性**：标题层级清晰；表格 / 表单 / 按钮布局规整；主要操作入口位置明确。
4. **R4 面向普通客户的易用性**：首页或醒目位置用简短文案说明项目是做什么的；核心功能入口一目了然；关键操作提供引导提示，无需培训即可上手。
5. **R5 状态补齐**：空状态、加载态、错误提示。
6. **R6 响应式**：兼容桌面与移动端。

## 硬性约束（本次改造的红线）

| 约束 | 说明 |
| --- | --- |
| 功能不变 | 不改 API 调用、不改 `useState` / `useEffect` 业务逻辑、不改数据流 |
| 路由/导航不变 | 保持 `App.tsx` 的 `useState<PageKey>` 单页切换，**不引入 react-router**；9 个页面 key 与顺序不变 |
| 零新依赖 | **不引入 MUI / Tailwind / antd / CSS-in-JS**，继续手写 CSS |
| 可改范围 | ✅ CSS 样式 ✅ JSX 结构与 `className`（为分区 / 对齐 / 状态展示调整 DOM 层级） ✅ 文案 ✅ 纯 UI state（如移动端导航开合） |
| 禁止 | ❌ 改动 `backend/` 下任何文件 ❌ 改动业务接口契约 |
| 后端 | 不动 `backend/` |

---

## 一、产品目标

**一句话目标**：在不改动任何功能与路由的前提下，把 9 个页面收敛到一套统一的视觉与交互规范，并把首页重构为"30 秒看懂 + 一键进入"的入口，使**第一次打开网站的普通客户无需培训即可完成一次研究**。

拆成 3 个正交子目标：

| 目标 | 判定标准 |
| --- | --- |
| G1 规范统一 | 全站同类元素（卡片 / 标题 / 按钮 / 表单行 / 状态标签）只存在 **1 种**主实现 + 有限变体；字号、圆角、间距、阴影全部来自 CSS 变量令牌，无硬编码散值 |
| G2 主操作可见 | 每个页面的主操作入口在**首屏之内**（1440×900 桌面 / 390×844 移动）不滚动即可看到 |
| G3 状态完备 | 每个会发起异步请求的页面都有「空 / 加载 / 错误（含重试）」三态，且三态样式统一 |

---

## 二、目标用户与使用场景

| 项 | 说明 |
| --- | --- |
| 主用户 | **第一次打开这个网站的普通客户**（非开发者、非本项目成员），无背景知识 |
| 他的三个疑问（按出现顺序） | ① 这是干嘛的？② 我该从哪点？③ 点了之后会发生什么？ |
| 使用场景 | 场景 A：打开首页 → 想快速判断"值不值得用" → 需要一句话说明 + 能力证明<br>场景 B：想试一次 → 需要一眼看到主入口，且知道要点哪个按钮<br>场景 C：点进某个功能页 → 需要知道**输入什么**（placeholder / 示例）和**点完之后会怎样**（引导提示）<br>场景 D：操作中出现等待 / 失败 / 无数据 → 需要明确反馈与"下一步怎么办" |
| 次要用户 | 已有使用者（回访）→ 需要快速定位「研究报告 / 历史运行」 |
| 关键设计含义 | 首页必须自解释；功能页必须"先给入口、再给说明"（当前是反过来的）；所有异步区块必须给反馈 |

---

## 三、现状诊断（代码级证据）

> 结论先行：**不是"没有设计规范"，而是"规范存在但没被贯彻，且存在两套并存实现"。**
> `index.css:7` 的 `:root` 已有一套完整的 Apple 风令牌（`--page/--surface/--surface-alt/--ink/--muted/--faint/--line/--accent/--danger/--ok/--warn/--radius-sm|md|lg/--max-width/--nav-height`）与明暗双主题（`index.css:48`）。问题是**下半部分又追加了一整层覆盖**。

### 3.1 CSS 结构问题

| 证据 | 位置 | 影响 |
| --- | --- | --- |
| **两套并存的 CSS 层** | 主设计系统 `1–2047` 行；追加层 `2099–2951` 行（约 850 行） | 追加层重复定义 `.badge`(.505 → .2104)、`.panel__title`(22px → **24px**)、`.panel__subtitle`(14px → **15px**)、`.run-card`、`.collapsible-card`、`.approval`、`.activity`、`.timeline`、`.input-card`、`.tool-call-card` 等。**后者静默覆盖前者**，改样式时极易改错层 |
| **12 个类被重复定义** | `.badge`(×3)、`.button`(×4)、`.panel__notes`(×4)、`.flow__step`(×4)、`.feature`(×4)、`.cta-banner`(×4)、`.timeline`(×4)、`.collapsible-card`(×3)、`.activity__head`(×3)、`.approval`(×3)、`.empty-state`(×3)、`.run-list__item`(×3) | 同一语义有多个来源，视觉必然漂移 |
| **死 CSS 约 120+ 行** | 在 tsx 中 0 引用的类：`.trace*`(11 个)、`.pipeline*`(4 个)、`.flow-card*`(4 个)、`.flow-grid`、`.flow-overview`、`.feature-index`、`.progress-arrow`、`.feature-index`、`.list`、`.plan`、`.plan__meta`、`.doc--column`、`.timeline__type`、`.timeline__id`、`.fold__body`、`.result-stack__head`、`.tool-call-card__reason` | 文件长度 2951 行的主要来源，干扰维护 |
| **阴影无令牌** | `0 1px 3px rgba(0,0,0,.04)`(.run-card)、`0 18px 40px rgba(0,0,0,.10)`(.flow__step:hover)、`0 24px 80px rgba(0,0,0,.28)`(.modal) 全为硬编码 | 深色主题下黑色阴影几乎不可见，悬浮层次丢失 |
| **字号无级数** | 实际出现的标题字号：`clamp(2.1–3.6rem)` hero / `clamp(1.8–2.6rem)` section h2 / `24px` panel title / `22px` / `20px` cta / `18px` feature h3 / `17px` report title / `16px` module·scenario h3 / `15px` flow__name·activity__title·module-section__title / `13px` plan__heading —— **10+ 种** | R1「字号层级」直接不达标 |
| **圆角混用** | 令牌 `8/12/18`，但另有硬编码 `5px`(code)、`8px`(nav-logo/.step__index)、`11px`(.module__icon)、`980px`(.button/.nav-link)、`999px`(badge)、`50%`(circle) | R1「圆角」不达标 |
| **间距无级数** | 卡片内 padding 实际值：`24 26 28` / `22 24 28` / `26 24 28` / `20 22` / `22 24` / `30 28` / `18 20` / `18 18 22` / `16 18` / `14 16` / `18 16` / `12 14` / `14` —— **13 种** | R2「间距整齐」不达标 |

### 3.2 组件实现分散问题

| 语义 | 现有实现数 | 具体类名 |
| --- | --- | --- |
| 卡片 / 容器 | **14 种** | `.feature`、`.module-card`、`.module`、`.scenario`、`.flow__step`、`.flow-card`(死)、`.step-card`、`.report-card`、`.input-card`、`.run-card`、`.tool-call-card`、`.collapsible-card`、`.metric`、`.stat-card` |
| 列表行 | **8 种** | `.metric`、`.settings-row`、`.param-row`、`.doc`、`.run-list__btn`、`.report-card`、`.timeline__item`、`.process-timeline__item` |
| 页面容器 | **3 种** | `.panel`（research/agent/knowledge/reports/evaluation/settings）、`.panel--clean`（workflow）、`.hero + .section`（dashboard/tutorial） |
| 状态标签 | 2 套 | `StatusBadge`（组件，`.badge--*`）+ `.badge badge--error` 硬编码（`ResearchPlanner.tsx:140`、`AgentRunner` 已改用组件） |

> 这直接解释了"各页面风格不一致"：**同一视觉语义在 9 个页面里有多套实现**。

### 3.3 布局与信息层级问题（R2/R3）

| 问题 | 证据 |
| --- | --- |
| **主操作被埋在说明文字之下** | `workflow` / `research` / `agent` 三页结构均为：`panel__header` → `module-section「这个模块能做什么？」` → `module-section「使用方式」` → **才轮到表单**。`ResearchWorkflow.tsx:342-421` 中间隔了 80 行说明 JSX，主按钮「启动研究工作流」在移动端约需滚动 **2.5–3 屏**才出现 → 违反 R3"主要操作入口位置明确" |
| **单列超长堆叠** | `ResearchWorkflow.tsx` 单页 8 个纵向区块（header / 能做什么 / 使用方式 / 启动区 / 实时进度 / 运行结果 / 人工确认 / 历史运行），全部同权重同色卡片，视觉无主次 → R2"杂乱堆叠" |
| **留白"两极分化"** | 营销页极空：`.hero` 上 padding `84px`（`index.css:240`）、`.section` 上下 `64px`（即 section 间 128px 节奏）、`.hero-facts` margin-top `56px`；工具页极挤：`.result-stack` gap 仅 `12px` → R2"过大留白或元素挤压" 同时命中 |
| **正文行长未受控** | `.panel` 内容宽约 1028px（`--max-width:1080` - padding），`.module-section__body`、`.panel__notes`、`.report__summary` 均无 `max-width`；14px 字号下每行 >100 字符 → 可读性差（仅 `.section-lede` 限了 660px） |
| **表格语义缺失** | `.settings-table`（`index.css:1889`）用 `div` + `grid: 220px 1fr` 模拟，20 行配置**无分组、无列头**，LLM / 搜索 / Agent / Embedding / CORS 参数混杂 → R3"表格布局规整"不达标 |
| **列表行渲染不一致（含缺陷）** | `RunHistory.tsx:87` 用 `.run-list__btn` 包裹（flex 行）；`Evaluation.tsx:108` 直接 `<li className="run-list__item">` 未包 `.run-list__btn`，而 `.run-list__item` 无 flex 定义（`index.css:1039`）→ 徽章 / 问题 / 时间挤成一行内联文本，视觉上像坏掉 |
| **错误样式不统一** | `RunHistory.tsx:80` 把请求错误渲染为 `<p className="hint">`（灰色辅助文字）而非 `.alert--error`；其余页面用 `.alert--error` → 用户以为"只是没数据" |
| **表单控件未样式化** | `KnowledgeBase.tsx:79` 用浏览器原生 `<input type="file">`，仅外层 `.upload` 是虚线框；`ResearchPlanner.tsx:123` / `AgentRunner.tsx:142` 用原生 `input[type=range]`（仅 `accent-color`） |

### 3.4 状态覆盖问题（R5）

| 证据 | 位置 | 问题 |
| --- | --- | --- |
| **无骨架屏 / 无 spinner** | 全站 `skeleton`、`spinner`、`loader` 关键词 **0 命中** | 加载态一律是灰色文字 |
| 加载态仅文字 | `Dashboard.tsx:143`「正在请求后端…」；`Evaluation.tsx:75` / `Reports.tsx:100` / `Settings.tsx:74`「加载中…」 | 无占位高度 → 内容到达时页面**跳动（CLS）** |
| **知识库首屏无加载态** | `KnowledgeBase.tsx:14` `documents` 初始 `[]`，请求中即渲染 `documents.length === 0` 分支 | 加载中直接显示"还没有文档"，**误导用户以为没有数据** |
| 空状态仅 1 页规范 | 仅 `Reports.tsx:103` 用 `.empty-state`；其余用 `.hint`：`KnowledgeBase.tsx:105`、`Evaluation.tsx:104`、`RunHistory.tsx:77`、`SearchBox`(`KnowledgeBase.tsx:174`) | 空状态样式 2 套、文案语气不统一 |
| **无重试入口** | `Reports.tsx:95`、`Settings.tsx:68`、`Evaluation.tsx:70` 只有 `.alert--error`，无「重试」按钮 | 失败后用户只能刷新整页 |
| 无空状态 | `ResearchPlanner`（idle 时结果区空白）、`AgentRunner`（idle 空白）、`ResearchWorkflow`（未运行时结果区空白） | 用户不知道该期待什么 |
| 唯一做得好的 | `Dashboard.tsx:145-166` 的错误态：状态徽章 + 错误码 + 可点击健康检查链接 + CORS 排查建议 | 应作为全站错误态模板 |

### 3.5 响应式问题（R6）

| 证据 | 位置 | 问题 |
| --- | --- | --- |
| **仅 2 个断点** | `index.css:2050`(≤860px)、`2060`(≤640px) | 无平板档（768–1199），无 hover 能力区分 |
| **移动端导航会溢出（P0）** | `.nav-menu`(`index.css:162`) 为 9 项横向 flex + `margin-left:auto`，`640px` 断点仅隐藏 `.nav-brand small`，**未处理 `.nav-menu`** | 375px 宽下 9 个中文标签（概览/研究工作流/研究规划/智能体/知识库/教程/研究报告/效果评估/系统设置）必然溢出或被压扁 |
| 无移动端图表/宽表策略 | `.progress-track` 有 `flex-wrap + overflow-x:auto`（`index.css:2243`）✅；但 `.settings-row` 仅 640px 断点转单列，`.report-panel` `max-height:70vh`，`.modal` `max-height:88vh`（`index.css:1863`）在移动端未转全屏 | 弹窗在手机上体验局促 |
| 触摸目标偏小 | `.link-button` 无内边距（`index.css:649`），`.nav-icon` 34px | 小于 44px 触摸目标建议 |

---

## 四、页面分级评估

**评分维度**（各 10 分）：视觉一致性 / 信息层级 / 留白与密度 / 状态覆盖 / 响应式 / 主操作可见性。

| # | 页面 | 视觉一致 | 信息层级 | 留白密度 | 状态覆盖 | 响应式 | 主操作 | **总分** | 结论 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | **dashboard 概览** | 8 | 6 | 5 | 7 | 7 | 6 | **6.5** | 视觉质量最高，但**信息架构需重做**：缺核心功能入口卡；「连接状态」技术信息对普通客户无意义且占据第 4 屏 | **P0** |
| 2 | **workflow 研究工作流** | 5 | 4 | 3 | 5 | 5 | 3 | **4.2** | 全站最差。主操作被 3 段说明压在 2.5 屏之下；8 个同权重区块单列堆叠；无空状态；历史运行错误用 `.hint` 伪装 | **P0（布局重做）** |
| 3 | **research 研究规划** | 5 | 5 | 4 | 5 | 6 | 4 | **4.8** | 与 workflow 同病：4 张 `.module-card` + 说明在表单之前；idle 无空状态；`.module-card` 与教程页 `.module` 是两套卡片 | **P0（布局重做）** |
| 4 | **evaluation 效果评估** | 5 | 5 | 6 | 5 | 5 | 5 | **5.2** | 结构简单但存在**真实渲染缺陷**（`.run-list__item` 未包 `.run-list__btn` → 行内容挤成一团）；空状态用 `.hint`；无重试 | **P1**（其中列表缺陷按 **P0 级**优先修） |
| 5 | **agent 智能体** | 5.5 | 5 | 4 | 5 | 6 | 4 | **4.9** | 同样"说明压主操作"；但结果区（run-card + 折叠卡 + 时间线）质量尚可 | **P1** |
| 6 | **knowledge 知识库** | 6 | 6 | 6 | **3** | 6 | 6 | **5.5** | 结构最简，但**状态覆盖最差**：首屏加载无态且误导；原生 file input 未样式化；空状态用 `.hint` | **P1** |
| 7 | **reports 研究报告** | 7 | 7 | 6 | 6 | 6 | 7 | **6.5** | 唯一规范使用 `.empty-state` 的页面；缺筛选/搜索/计数、缺错误重试、加载仅文字 | **P1** |
| 8 | **tutorial 教程** | 7.5 | 7 | 5 | 7 | 6 | 6 | **6.4** | 视觉丰富但**与 dashboard 内容重叠**；hero 上行距 `64px` 偏空；指引文案与实际按钮名不符（"确认/修改" ↔ 实际"批准并生成报告/终止任务"） | **P2** |
| 9 | **settings 系统设置** | 6 | 5 | 6 | 5 | 6 | 6 | **5.7** | 20 行配置无分组、无列头；对普通客户属开发者页，应弱化入口或明确标注"高级" | **P2** |
| — | **全局导航 / 设计令牌** | 4 | 5 | — | — | **2** | 6 | **4.3** | 两套 CSS 并存；移动端导航溢出；阴影/字号/间距缺令牌 | **P0** |

### 页面结论汇总

| 类别 | 页面 |
| --- | --- |
| **P0 需重做（3 页）** | `dashboard`（信息架构）· `workflow`（布局分层）· `research`（布局分层） |
| **P0 全局项** | 设计令牌统一 + CSS 去重 + **移动端导航** |
| **P1 对齐规范 + 补状态（4 页）** | `agent` · `knowledge` · `evaluation`（含 1 项必修缺陷）· `reports` |
| **P2 轻量对齐 + 文案（2 页）** | `tutorial` · `settings` |

---

## 五、用户故事

| ID | 用户故事 | 关联需求 |
| --- | --- | --- |
| US-1 | **作为**第一次打开网站的普通客户，**我希望**在首页第一屏用一句话看懂"这个工具能把问题变成带出处的研究报告"，**以便**在 30 秒内决定是否值得继续用。 | R4 |
| US-2 | **作为**想试一次的新用户，**我希望**首页直接列出「研究工作流 / 智能体 / 知识库 / 研究报告」四个入口并标明哪个是主入口，**以便**不用猜测该点哪个。 | R4 / R3 |
| US-3 | **作为**准备启动研究的用户，**我希望**打开功能页就能看到输入框和主按钮，且输入框上写着该写什么、按钮旁边写着点完会发生什么，**以便**不用先读完整页说明才能动手。 | R3 / R4 |
| US-4 | **作为**使用中遇到等待或失败的用户，**我希望**看到明确的加载 / 空 / 错误状态和「重试」按钮，并知道数据到底是没有还是没加载出来，**以便**判断是自己操作错了还是系统问题。 | R5 |
| US-5 | **作为**在手机上查看报告的用户，**我希望**导航能收起来、卡片单列、表格改为上下排列、按钮撑满宽度，**以便**在手机上也能完整读完一份报告。 | R6 / R2 |
| US-6 | **作为**回访用户，**我希望**全站标题大小、卡片圆角、按钮样式保持一致，**以便**不用重新适应每个页面的视觉规则。 | R1 |

---

## 六、需求池

### P0 — Must have（不做则改造失败）

| ID | 需求 | 对应 | 验收标准 |
| --- | --- | --- | --- |
| **P0-1** | **建立统一设计令牌**：在 `:root` 补齐 `--space-*`、`--text-*`、`--shadow-*`、`--radius-*`（含 `--radius-pill`），并为 `:root[data-theme="dark"]` 提供对应阴影值；所有硬编码字号/圆角/间距/阴影改为引用令牌 | R1 | 全站卡片 padding 取值收敛到 ≤3 种；标题字号收敛到 4 级；圆角只用 3 档 + pill；阴影只用 3 档 |
| **P0-2** | **合并/去重 CSS**：删除追加层（`2099–2951`）中对同名类的重复定义，保留唯一来源；清理 0 引用的死类（`.trace*`、`.pipeline*`、`.flow-card*`、`.flow-grid`、`.flow-overview`、`.feature-index`、`.progress-arrow`、`.list`、`.plan`、`.plan__meta`、`.doc--column`、`.timeline__type`、`.timeline__id`、`.fold__body`、`.result-stack__head`、`.tool-call-card__reason`） | R1 | 同类名在全文件只定义 1 次；CSS 行数下降 ≥ 15% 且无视觉回归 |
| **P0-3** | **首页（dashboard）重构为入口页**：<br>① 首屏一句话说明"这是什么"<br>② 新增 4 张「核心功能入口卡」（研究工作流[主] / 智能体 / 知识库 / 研究报告）<br>③ 把「连接状态」技术明细（环境/版本/延迟/UTC 时间）从首页移到「系统设置」，首页仅保留一个轻量状态点 | R4 / R3 | 首屏内可见一句话说明 + 4 个入口；首页区块数由 4 个降至 ≤3 个 |
| **P0-4** | **功能页调整为"主操作优先"结构**：`workflow` / `research` / `agent` 三页把 `panel__header` → **主操作区（输入 + 主按钮）** → 折叠的「这个模块能做什么 / 使用方式」 → 结果区；主按钮在 1440×900 首屏内可见，移动端 ≤1 屏 | R3 / R2 | 三页主按钮均满足"首屏可见"；说明文案改为可折叠或移至页面底部 |
| **P0-5** | **移动端导航可用**：≥1024px 平铺 9 项；768–1023px 收为「核心 4 项 + 更多」；<768px 收为汉堡按钮 + 抽屉（单列、当前页高亮、点选后自动收起）。仅新增 UI 开合 state，不改导航结构与页面 key | R6 | 375px 宽下无横向溢出；所有 9 个入口均可到达 |
| **P0-6** | **修复 evaluation 最近运行列表渲染**：为 `<li className="run-list__item">` 补 `.run-list__btn` 等价容器，使其与「历史运行」行完全一致（徽章 / 问题 / 时间 三列对齐、问题单行省略） | R3 / R1 | 两处列表行视觉像素级一致 |
| **P0-7** | **错误态统一 + 补重试**：所有请求错误统一用 `.alert--error`（禁用 `.hint` 渲染错误，修 `RunHistory.tsx:80`）；`Reports` / `Evaluation` / `Settings` / `KnowledgeBase` 错误块内提供「重试」按钮；沿用 `Dashboard` 错误态模板（徽章 + 错误码 + 下一步建议） | R5 | 全站错误态样式唯一；每个失败区块均有重试入口 |

### P1 — Should have

| ID | 需求 | 对应 | 验收标准 |
| --- | --- | --- | --- |
| **P1-1** | **加载态升级为骨架/占位**：为卡片列表、表格、结果区提供带固定高度的骨架块，消除内容到达时的页面跳动 | R5 | 4 个有加载态的页面全部使用骨架；加载→完成过程无 CLS |
| **P1-2** | **知识库补首屏加载态**：区分"加载中 / 确实为空 / 加载失败"三种情况，不再在加载中显示"还没有文档" | R5 | 首屏不再出现误导性空状态 |
| **P1-3** | **统一空状态组件**：抽出统一空状态（图标 + 标题 + 一句说明 + 下一步按钮），替换现有 4 处 `.hint` 式空状态（知识库文档列表 / 检索无结果 / 效果评估运行记录 / 历史运行）；为 `research`、`agent`、`workflow` 的 idle 态补"未开始"引导块 | R5 / R4 | 全站空状态样式与文案结构一致 |
| **P1-4** | **表单单行化与控件样式化**：统一 `.field` 间距、label 位置、helper/hint 位置；为原生 `input[type=file]` 提供自定义触发按钮 + 文件名回显；`input[type=range]` 统一高度与数值展示 | R3 / R1 | 表单行在桌面呈"label 左 / 控件右"或统一上下结构，间距一致 |
| **P1-5** | **系统设置分组**：把 20 行配置按「应用 / LLM / 搜索 / Agent / 研究工作流 / Embedding / 安全(CORS)」分 7 组，每组带头部与小标题；敏感字段（Key）用状态徽章而非纯文本 | R3 / R2 | 配置项分组可扫读；单屏信息量下降 |
| **P1-6** | **正文行长受控**：为 `.module-section__body`、`.panel__notes`、报告正文等长文本容器加 `max-width: 68ch`（约 720px），超出部分在宽屏下左对齐留白 | R2 / R3 | 1440px 下正文每行 ≤ 90 字符 |
| **P1-7** | **卡片体系统一**：14 种卡片收敛为「基础卡 / 可折叠卡 / 交互卡（可点）」3 类 + 有限语义变体（统计卡、状态卡），统一 padding、圆角、边框、hover 阴影 | R1 / R2 | 同类卡片在任意两页视觉一致 |
| **P1-8** | **导航与全局壳层**：`.nav-inner` / `.shell` 使用同一容器宽度令牌；导航高亮态从"仅加粗"升级为"加粗 + 底部指示条 / 胶囊底"；`.link-button` 补最小触摸高度 | R1 / R3 / R6 | 桌面/移动容器边距一致；导航当前页可明确识别 |
| **P1-9** | **报告列表增强**：加条目计数、按时间倒序标识、页面内搜索框（纯前端过滤已有 `runs` 数组，不改 API）、加载更多；移动端 modal 转全屏 | R3 / R6 | 报告数 >20 时仍可快速定位；手机上报告可全宽阅读 |

### P2 — Nice to have

| ID | 需求 | 对应 |
| --- | --- | --- |
| **P2-1** | 统一页面垂直节奏：营销页 `.section` 由 `64px` 收敛至 `40–48px`，`.hero` 上 padding 由 `84px` 收敛至 `56–64px`；工具页区块间距由 `12px` 提升至 `16–20px` | R2 |
| **P2-2** | 教程页文案纠偏：把"点击「确认」/「修改」"改为实际按钮名「批准并生成报告 / 终止任务」；合并与 dashboard 重复的能力介绍 | R4 |
| **P2-3** | 系统设置入口降级：导航中标为次要（弱化色），或在设置页顶部标注"面向管理员 / 高级配置" | R4 |
| **P2-4** | 触摸目标合规：所有可点击元素 ≥44×44px；`.fold__summary`、`.settings-row` 交互行补 hover/focus 反馈 | R6 |
| **P2-5** | 深色主题校准：为深色模式单独定义阴影与边框透明度，替换硬编码黑色阴影；校验所有 `rgba(0,113,227,…)` 硬编码强调色在深色下的对比度 | R1 |
| **P2-6** | 动效克制化：`.flow__step:hover` 的 `scale(1.05)` 改为 `translateY(-2px)`；仅在支持 hover 的设备上生效（`@media (hover:hover)`） | R2 / R6 |

---

## 七、易用性设计（R4）

### 7.1 首页「这个项目是做什么的」文案建议

替换/新增以下文案（保持简洁，中文，每句 ≤ 45 字）：

| 位置 | 建议文案 |
| --- | --- |
| 眉标 eyebrow | `AI 研究工作台` |
| 主标题 H1 | `提一个问题，拿到一份有出处的研究报告。` |
| **一句话说明**（核心，新增） | `输入一个研究问题，智能体自动完成「理解 → 规划 → 检索 → 分析 → 核对 → 写报告」，过程实时可见，写报告前会停下来等你确认，每条结论都标了来源。` |
| 主按钮 | `开始研究` → `workflow` |
| 次按钮 | `3 分钟上手教程` → `tutorial` |
| 事实条（保留精简） | `7 步研究流水线` · `实时进度` · `写报告前可确认` · `结论可溯源` |
| 原「连接状态」区块 | 从首页移除；首页仅保留一行轻量状态：`后端已连接` / `后端未连接，点此排查`（点击跳「系统设置」） |

### 7.2 核心功能入口如何组织

**首页首屏下方紧接一个 4 卡入口区**（`.entry-grid`，桌面 4 列 / 平板 2 列 / 移动 1 列）：

| 序 | 入口 | 图标字 | 一句话说明 | 视觉权重 |
| --- | --- | --- | --- | --- |
| 1 | **研究工作流** | 程 | 跑一遍完整研究，写报告前可先确认方向。 | **主入口**：accent 边框 + accent 底色 + 「推荐」角标 |
| 2 | 智能体 | 体 | 给个任务，自动查资料并给出带引用的答案。 | 次级：基础卡 |
| 3 | 知识库 | 库 | 上传自己的资料，让研究能引用内部文档。 | 次级：基础卡 |
| 4 | 研究报告 | 报 | 查看已完成的研究报告，支持全文阅读。 | 次级：基础卡 |

**次级入口**（一行文字链，放入口区下方）：`研究规划` · `效果评估` · `系统设置` · `教程`

组织原则：
1. **1 主 3 次**——主入口唯一且视觉最强，避免 9 个入口平权。
2. **按用户旅程排序**——先"跑研究"，再"自动跑/喂资料/看结果"。
3. **每卡一句话，动词开头**——不出现"模块""功能"等系统语言。
4. **导航与入口卡同名**——降低认知负担（导航不变，卡片只是快捷方式）。

### 7.3 关键操作的引导提示

| 关键操作 | 位置 | 引导文案（placeholder / 提示条 / 说明） |
| --- | --- | --- |
| **启动研究**（`workflow`） | 输入框 placeholder | `用一句话写清研究问题，例如：研究 2026 年 AI Agent 岗位的核心技术要求。` |
| | 输入框下方 hint | `至少 8 个字。写得越具体，计划越准。` |
| | 主按钮旁提示条 | `点「启动研究工作流」后，进度会实时刷新；写报告前会停下来等你确认，不会直接输出最终报告。` |
| | 未满足条件时 | 按钮禁用 + 提示从灰色变 accent：`还差 N 个字就能开始` |
| **上传文档**（`knowledge`） | 上传区主文案 | `把 PDF / Markdown / TXT 拖到这里，或点击选择文件` |
| | 上传区副文案 | `单文件 ≤ 10MB。上传后会自动解析切块，状态变为「就绪」即可被研究引用。` |
| | 上传中 | `正在解析、切块、向量化…通常几秒到几十秒，可以先去别的页面。` |
| | 上传成功 | `已摄入《文件名》，共 N 个片段。可以在下方「检索测试」输入关键词验证能否命中。` |
| | 空列表 | `还没有资料。上传一份 Markdown 文件试试，之后的检索就能引用它。` + `选择文件` 按钮 |
| **运行 Agent**（`agent`） | 输入框 placeholder | `描述要研究的问题，例如：对比 A 与 B 两种方案的优缺点，并给出证据。` |
| | 步数控件说明 | `最大步数：6（越多查得越细，也越慢）` |
| | 主按钮旁提示 | `Agent 会自己决定搜索、抓取网页、计算。达到最大步数或得出结论后停止，并给出带引用的答案。` |
| | 运行中 | `Agent 执行中…正在第 N 步，这一步在调用「工具名」。` |
| **批准报告**（`workflow` 中断点） | 确认区标题 | `证据已收集完毕，请确认是否生成报告` |
| | 确认区说明 | `智能体已整理出核心结论和引用来源。确认后会生成最终报告；也可以在下面补充要求，让它按你的方向调整。` |
| | 补充意见 placeholder | `可选。例如：多写一些落地建议，少写行业背景。` |
| | 主按钮 | `批准并生成报告` |
| | 次按钮 | `终止本次研究`（并在点击后二次确认） |

统一引导形式（3 种，不新增第 4 种）：
1. **placeholder**——告诉我该输入什么（必带示例）
2. **hint 行**——告诉我校验规则 / 当前状态
3. **tip 提示条**（`.tip`，已有类）——告诉我"点完之后会发生什么"，每页 ≤1 个

---

## 八、状态清单（R5）

图例：✅ 已有且规范 · ⚠️ 已有但不规范（样式/文案需统一） · ❌ 缺失

| 页面 | 空状态 | 加载态 | 错误态 | 需补的动作 |
| --- | --- | --- | --- | --- |
| **dashboard** | 不适用 | ✅ 健康检查（文案「正在请求后端…」）→ ⚠️ 改骨架 | ✅✅ 全站最佳（徽章+错误码+排查建议） | 加载态改骨架；错误态样式抽为全站模板；移除技术明细 |
| **workflow** | ❌ 未运行时结果区空白 → 需「还没开始」引导块 | ✅ 按钮态「执行中…」；❌ 结果加载无态 | ✅ 启动/恢复/刷新失败 `.alert--error` | 补 idle 空状态；**历史运行错误由 `.hint` 改为 `.alert--error` + 重试**（`RunHistory.tsx:80`）；补事件流空态「等待事件…」→ 规范 |
| **research** | ❌ idle 结果区空白 → 需「输入问题后这里会显示计划」 | ✅ 按钮态「生成中…」；❌ 结果区无骨架 | ✅ `.alert--error` + 错误码 | 补 idle 空状态；补结果骨架；统一 `.badge--error` → `StatusBadge` |
| **agent** | ❌ idle 结果区空白 → 需「运行后这里会显示答案与执行轨迹」 | ✅ 按钮态「Agent 执行中…」；❌ 结果区无骨架 | ✅ `.alert--error` + 错误码 | 补 idle 空状态；补执行轨迹骨架 |
| **knowledge** | ⚠️ 文档列表空用 `.hint`（应升级为统一空状态）<br>⚠️ 检索无结果「没有命中任何片段。」（应给建议） | ❌ **首屏列表无加载态（会误显示"还没有文档"）**<br>✅ 上传中提示 | ✅ `listError` + 上传消息 + 检索错误 | **优先补首屏加载态**；空状态升级 + 加「上传」按钮；检索无结果补建议（`换个关键词，或先上传资料`）；错误 + 重试 |
| **tutorial** | 不适用（静态页） | 不适用 | 不适用 | 无（仅文案与留白调整） |
| **reports** | ✅ `.empty-state`（全站唯一规范实现） | ⚠️ 仅文字「加载中…」 | ⚠️ `.alert--error` 但无重试；报告详情加载失败仅提示 | 加载改骨架；**补「重试」按钮**；补"列表为空但有失败记录"的区分提示；详情加载失败补重试 |
| **evaluation** | ⚠️ 「暂无运行记录。」用 `.hint` | ⚠️ 仅文字「加载中…」（整个统计区一起消失） | ⚠️ `.alert--error` 无重试 | 空状态升级（含"去跑一次研究"按钮）；统计卡改骨架；补重试；**修复 `.run-list__item` 渲染缺陷** |
| **settings** | ⚠️ 无显式空态（config 为 null 且非 loading/error 时整块空白） | ⚠️ 仅文字「加载中…」 | ⚠️ `.alert--error` 无重试 | 补加载骨架（表格行占位）；补错误重试；补"配置为空"空态 |
| **全局** | — | — | — | 定义 3 个共享状态块：`.state-empty` / `.state-loading`（骨架）/ `.state-error`（含重试），全站复用 |

---

## 九、响应式要求（R6）

### 三档断点（替换现有 2 档）

| 档位 | 宽度 | 容器 | 栅格 |
| --- | --- | --- | --- |
| Desktop | ≥ 1024px | `min(1120px, 100% - 48px)` 居中 | 12 列，卡片按 3–4 列 |
| Tablet | 768–1023px | `100% - 40px` | 8 列，卡片 2 列 |
| Mobile | < 768px | `100% - 32px` | 4 列，卡片 1 列 |

> 为控制回归风险，`--max-width` 可从现有 `1080px` 保守起步，仅统一内边距。

### 分组件策略

| 组件 | Desktop | Tablet | Mobile |
| --- | --- | --- | --- |
| **导航** | 9 项平铺，胶囊高亮 + 当前页指示 | 4 个核心项（概览/研究工作流/知识库/研究报告）+「更多」下拉 | 汉堡按钮 → 全屏抽屉，单列 ≤44px 行高，当前页高亮，选中后自动收起；品牌区保留 logo + 名称 |
| **Hero** | 上 padding 56–64px，H1 `clamp(2rem,4vw,3rem)` 居中 | 字号降一档，居中 | 上 padding 32px，字号 `1.75rem`，按钮**全宽堆叠**，事实条 2×2 |
| **卡片网格** | 入口卡 4 列 / 能力卡 3 列 / 统计卡 4 列 | 2 列 | 1 列（统计卡 2×2 保留） |
| **表单** | `.field` 上下结构；`.field--inline` 为 label 左 + 控件右（range 宽 ≤220px） | 同桌面 | 一律上下结构；label 在上、控件占满宽；按钮全宽 |
| **表格（设置页）** | `.settings-row` 两列 `200px 1fr`，分组头吸顶 | 两列 `160px 1fr` | **单列转换**：label 在上（小号弱色），值在下；行内 `padding` 增至 14px |
| **数据列表（历史运行 / 最近运行）** | 徽章 / 问题（弹性+省略）/ 时间 三列对齐 | 同桌面，问题占满 | 时间换行至第二行，徽章与问题同排 |
| **过程可视化** | `.progress-track` 单行 7 节点，居中 | 允许换行 | **横向滚动**（保留 `overflow-x:auto` + 边缘渐隐提示），节点不换行 |
| **长文 / 报告** | 正文 `max-width: 68ch` 左对齐；报告 modal `760px` | modal `100% - 40px` | **modal 转全屏**（`inset:0`，`max-height:100vh`，头部固定、内容滚动，关闭按钮 ≥44px） |
| **工具调用 / 参数表** | `.param-row` `100px 1fr` | 同桌面 | 单列：参数名在上、值在下 |
| **横向溢出兜底** | — | — | `pre`、`.tool-call-card__output`、`.markdown-body table` 统一 `overflow-x:auto`；`body` 不得出现横向滚动 |
| **触摸/Hover** | 保留 hover 抬升与 `scale` | 禁用 `scale` 类动效 | 所有 `:hover` 效果包在 `@media (hover:hover)` 内；可点元素 ≥44×44px |

---

## 十、交付与验收方式

| 项 | 说明 |
| --- | --- |
| 交付 | 仅 CSS + JSX 结构 + 文案改动，无新增依赖，`backend/` 零改动 |
| 验收 1 | 9 个页面在 1440×900 / 834×1112 / 390×844 三档无横向滚动、无内容截断 |
| 验收 2 | 每个页面主操作入口在首屏内可见（除 `settings` 只读页） |
| 验收 3 | 断网（关闭 backend）状态下逐页检查：均出现统一错误块 + 可重试 |
| 验收 4 | 明暗主题切换后，卡片层次、状态色、阴影均可辨识 |
| 验收 5 | 同类元素（卡片 / 标题 / 按钮 / 状态徽章）在任意两页视觉一致；CSS 中同名类仅定义 1 次 |

---

## 十一、待确认问题

> 均已给出**默认决策**，不阻塞开发；如需调整请指出。

| # | 问题 | 默认决策 |
| --- | --- | --- |
| Q1 | 首页「连接状态」的技术明细（环境 / 版本 / 延迟 / UTC 时间）是否保留在首页？ | **移入「系统设置」**，首页只保留一行状态点 + 排查入口 |
| Q2 | 移动端导航是否允许折叠为汉堡抽屉？需在 `App.tsx` 新增 1 个 UI 开合 state（不涉及路由与业务逻辑） | **允许**（仅新增 UI state，`PageKey` 与导航结构不变） |
| Q3 | 是否清理约 120 行死 CSS（`.trace*` / `.pipeline*` / `.flow-card*` / `.feature-index` 等 0 引用类）？ | **清理**（经全量 tsx 引用核查，不影响任何页面） |
| Q4 | 容器宽度是否从 `1080px` 调整为 `1120px`？ | **保持 1080px**（最低回归风险），仅统一内边距与栅格 gap |
| Q5 | 深色模式是否单独定义一套阴影令牌？ | **是**（当前硬编码黑色阴影在深色下几乎不可见） |
| Q6 | 教程页"点击「确认」/「修改」"与实际按钮名不一致，是否允许改文案？ | **改**，对齐为「批准并生成报告 / 终止本次研究」 |
| Q7 | `research` / `agent` 页的「这个模块能做什么？」4 张说明卡是否直接删除？ | **不删除**，改为**页面底部可折叠区块**（默认收起），保留信息但不抢占主操作位置 |
| Q8 | 是否引入 CSS 变量以外的命名约定（如 BEM 收敛）？ | **本次不强制重构类名**（降低改动面），仅要求"同名类只定义 1 次 + 新增样式一律用令牌" |
